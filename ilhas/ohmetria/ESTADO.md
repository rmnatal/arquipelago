---
ilha: ohmetria
estado: nascendo
prioridade: 1
piso: abaixo            # abaixo | atingido — ver seção 21 do ARQUIPELAGO.md
urls_publicadas: 0
primeira_indexacao: null
ultima_execucao: 2026-09-15T11:17Z
executando_desde: null   # reserva orfa de 15/09 LIMPA em 18/09/2026 — a execucao que reservou morreu no limite semanal e nunca fechou
rede: aberta em 2026-09-18   # 20.1: ohmetria.com.br entrou na lista de Dominios permitidos da conta. MEDIDO em 18/09 as 12h35Z: HTTP 200. O bloqueio de 15/09 acabou.
bloco_atual: |
  BLOCO 4 ENTREGUE — a F1 existe como REGRA e ainda nao como PAGINA. ferramentas/f1-referencia.py (o cerebro: entrada -> resposta inteira, ja em frase acentuada), dados/f1-respostas.json (os 144 estados varridos mais a tabela pre-renderizada de 24 linhas, publicar false), ferramentas/gerar-f1.py, ferramentas/teste-f1.py e ferramentas/mutacoes-f1.py. NADA foi ao ar: esta ilha nao tem site, nao tem Sync e nao tem /status, e dizer que verificou no ar seria inventar.
  A ESCOLHA DA ILHA: PRIMEIRA TENTADA, uma corrida perdida e ganha no rebase. Os cinco ESTADO.md parseiam em yaml.safe_load e os cinco tinham executando_desde null, que pela 1.1 ja significa que nao ha bloco da Fundacao vivo — nao houve reserva vencida para o git desempatar. Pela 18.1 li o topo dos cinco PROMPT.md antes da rotacao e NENHUMA ilha tem despacho corretivo com item acionavel aberto: o da aquametria de 14/09 esta CUMPRIDO E CONFERIDO NO AR, o da clubedomosaico de 14/09 esta FECHADO desde as 21h17Z daquele dia, o da robometria de 14/09 idem, e os desta ilha e o da jornadafly sao despachos de NASCIMENTO, que carregam a fila inteira e por isso nao cabem na 18.2. Sobrou a rotacao da secao 1 e a ohmetria tinha a ultima_execucao mais antiga (21h20Z de 14/09, contra 22h16Z, 23h35Z, 23h35Z e 00h01Z). Meu push de reserva foi RECUSADO porque outra execucao reservou a aquametria as 11h17Z; rebase sem force push e a reserva da ohmetria entrou na segunda tentativa, as 11h19Z. Nenhum branch claude e nenhum PR aberto.
  REDE PELA 20.2, RETESTADA E NAO HERDADA: TRES passadas, ohmetria.com.br e www.ohmetria.com.br em 000 nas tres, com aquametria.com.br e robometria.com.br em 200 nas mesmas tres. Seis medicoes de bloqueio contra seis de controle verde, igual as tres execucoes anteriores. Segue bloqueada, e bloqueada_por segue null de proposito: o que a rede trava e o 3b em diante, nao o bloco de arquivo.
  POR QUE A F1 NAO TEM PHP, e isto e decisao registrada e nao pendencia esquecida: sem casca (bloco 3b, trabalho de navegador do Raphael), um snippet so poderia ser medido por um render de bancada que serve MENOS do que o site serve — a cicatriz que a secao 8 do contrato mais cobra, paga tres vezes pela Robometria. A regra nasceu onde pode ser conferida hoje, e o PHP, quando nascer, e TRADUTOR da referencia, frase a frase, como a Robometria faz entre cobertura-r1.py e robometria-r1.php.
  O ACHADO DO BLOCO, e ele estava num campo que o bloco 2 ja tinha gerado: pedidos_que_nao_fecham devolve mais_perto_que_fecha para TODO pedido que nao fecha, e servir esse campo sempre do mesmo jeito publicaria, em 17 dos 144 estados, A RESPOSTA QUE QUEIMA O MODULO. Porque abaixo_do_pedido true quer dizer que NADA na montagem alcanca o minimo que aquele modulo estabiliza, e o valor devolvido e o menos ruim de um conjunto que e todo ruim. A tela passou a separar tres coisas onde a especificacao previa duas: NAO FECHA SOBE (36 estados, existe valor acima, a pagina o nomeia e diz o que se perde), NAO FECHA SO ABAIXO (17 estados, a pagina NAO oferece valor e manda trocar o modulo ou mudar a quantidade), e NAO FECHA EM NENHUMA (30 estados, as seis montagens de tres). E a assimetria de custo da secao 10 aplicada a um campo que ja existia: quem separa as duas causas no codigo separa as duas no texto.
  O PISO DE COMPRA COM BANCO VAZIO E DERIVADO DA RESPOSTA, nao do item: a 25.2 da o piso por marca mais modelo e com zero modulos nao existe item. A busca sai da impedancia em que o modulo precisa estabilizar, o molde vem de esquema-banco.json e nunca e digitado na ferramenta, e a bancada refabrica cada URL do molde e compara. Sem rel sponsored, porque busca crua nao rende comissao. 91 estados com piso, 53 SEM piso — e os 53 dizem por que, porque mandar quem nao tem montagem valida para uma busca de modulo seria vender uma coisa no lugar de outra.
  A CONTA QUE EU IA PUBLICAR ESTAVA ERRADA, e ela esta escrita na especificacao com o erro ao lado: a primeira versao da secao 7 dizia 47 estados sem piso — somei 30 mais 17 de cabeca e esqueci os 6 estados SEM PEDIDO das montagens de tres. Sao 53. O conserto nao foi trocar o numero: a bancada passou a LER a prosa da secao 7 e conferir os cinco vereditos e as duas contas do piso contra a varredura, com quatro mutacoes provando que ela morde. E a terceira conta que a secao 8 pede, e o defeito que ela impede e o mais barato de cometer.
  AS DUAS COISAS QUE AS MUTACOES ACHARAM, as duas fechadas antes do commit e as duas da mesma familia. (1) A bancada nao conferia a lista de modulos POR LIGACAO: com o banco vazio a afirmacao passava sobre lista vazia, e com um modulo dentro a linha da ligacao podia continuar dizendo que nao havia nenhum. E o mundo que o esquema permite e o banco ainda nao tem, e so a mutacao que PRODUZ o mundo o enxerga. (2) A referencia nao conferia o banco de casos contra o vocabulario do esquema: apagando impedancia_de_bobina, a F1 continuava respondendo a partir dos casos de ontem. Duas metades contando a mesma coisa sem nunca se falarem.
  E A REGUA DE ACENTO PEGOU UM DEFEITO QUE NAO ERA DE ACENTO: a frase da prestacao de contas citava dados/modulos.json NA TELA. O caminho de arquivo do repositorio na pagina e a ilha falando com o visitante no vocabulario de quem a escreveu, e a regra da secao 8 sobre texto de tela acentuado foi o unico portao que o viu.
  BANCADA, 0 falha: teste-f1 8.262 afirmacoes (varrendo os 144 estados no CORPO, com a aritmetica reescrita a mao em Fraction para poder discordar do gerador), impedancias --conferir 73, validar-banco sem erro com o aviso de vocabulario_sem_lastro 5 de 5, mutacoes-banco 39 de 39. MUTACOES DA F1: 41 de 41 — 4 mundos que tem de passar (banco vazio, 1 modulo, 2 modulos e um modulo em TODAS as impedancias), 28 quebras cada uma declarando a frase que espera ouvir, 4 mortes (chave que some da fonte tem de matar a referencia, nunca virar literal), 4 sobre a prosa da especificacao e o autoteste da propria bateria.
  PROXIMO PASSO: a F2 como REGRA, pelo mesmo desenho — e com uma diferenca que muda tudo, porque formula-volume-caixa-selada esta aguarda_validacao e faixa-qtc-alvo esta pendente, entao a F2 NAO pode publicar litragem enquanto a leitura nao abrir. O que ela pode fazer hoje e o que a F1 fez com o banco vazio.
---

# Estado da ilha OHMETRIA

Espelho legível do estado do projeto. **Nunca guarde credencial aqui.**

## Infraestrutura
- Domínio: **`ohmetria.com.br` — REGISTRADO E PAGO em 14/09/2026 às 15h00:24Z.** Medido pelo RDAP oficial nesta execução (`rdap.registro.br/domain/ohmetria.com.br`): `status: active`, expira em **14/09/2028**, contato técnico `RAMNA14`. **O passo 1 da seção 11 está CUMPRIDO** — a linha anterior deste arquivo, que dizia "pendente de registro", foi escrita antes das 15h e estava errada desde então.
- **Rede da nuvem: BLOQUEADA.** `ohmetria.com.br` não está na lista "Domínios permitidos" (seção 20.1). Despacho de prioridade ALTA para o Raphael em `dados/despachos.md`, no mesmo item da jornadafly.
- DNS: ainda nos nameservers de estacionamento do registro.br (`a.auto.dns.br`, `b.auto.dns.br`), **sem registro A** — `ohmetria.com.br` não resolve para endereço nenhum. É o normal para domínio registrado hoje e ainda não apontado.
- Hospedagem: domínio adicional no mesmo plano da HostGator, **com raiz própria** `/home3/<usuário>/ohmetria` — nunca compartilhando raiz com outra ilha. Pendente.
- Nameservers: `ns604` e `ns605.hostgator.com.br`, **só DEPOIS do domínio adicional existir no cPanel** — invertendo a ordem o registro.br recusa. Pendente.
- Search Console: propriedade de **Domínio** (não prefixo de URL), **antes do WordPress**. Pendente.
- WordPress: pendente.
- Snippet de Sync: `Ohmetria Sync`, token novo gerado na hora. Pendente.
- Etiquetas do Mercado Livre: `ohmetriaf1`, `ohmetriaf2`, `ohmetriaf3` — **ainda não existem** na conta RMNATAL. Criar etiqueta funciona por script; **gerar o link, não** (reCAPTCHA — é trabalho do Raphael, seção 7).

## O que já foi entregue
- **Bloco 1 — corpus de buscas paramétricas** (14/09/2026, 17h17Z–17h50Z): `dados/corpus-buscas.md`, os três eixos medidos, `publicar: false`. Nenhuma URL publicada, nenhum item de banco criado, nada foi ao ar — esta ilha ainda não tem site.
- **Bloco 2 — especificação das ferramentas e constantes** (14/09/2026, 19h16Z–20h00Z): `dados/especificacao-calculadoras.md` (F1, F2 e F3 com consulta-alvo, classificação de SERP pela 14.9, entradas, saídas, portão da seção 7, o que cada uma recusa responder, e o portão de medição que cada bloco tem de trazer), `dados/constantes.json` (**18 constantes**: 6 publicáveis, 3 publicáveis como desacordo, 1 aguardando validação, **8 pendentes**, mais `pendencias_de_leitura` com os quatro documentos que promovem cada uma), `ferramentas/impedancias.py` e `dados/impedancias-alcancaveis.json` (**24 casos contados**). Todos com `publicar: false`. **Nada foi ao ar.**

- **Bloco 4 — a F1, construída como REGRA e ainda não como PÁGINA** (15/09/2026, 11h17Z–12h00Z): `ferramentas/f1-referencia.py` (o cérebro: entrada → resposta inteira, em frase acentuada), `dados/f1-respostas.json` (**144 estados** varridos, com a tabela pré-renderizada de 24 linhas, `publicar: false`), `ferramentas/gerar-f1.py`, `ferramentas/teste-f1.py` (**8.262 afirmações, 0 falha**) e `ferramentas/mutacoes-f1.py` (**41 mutações, 41 certas**). O que cada coisa é e onde está: seção 7 de `dados/especificacao-calculadoras.md`. **Nada foi ao ar** — esta ilha não tem site, não tem Sync e não tem `/status`.
- **Bloco 3 — modelo do banco** (14/09/2026, 21h20Z–22h05Z): `dados/esquema-banco.json` (8 entidades, 7 vocabulários, a escada de fontes com o canal de leitura, os campos que exigem declaração de origem pela 26.2, o piso de compra derivado e a lista dos mundos que o esquema permite e o banco não tem), `dados/modulos.json` e `dados/alto-falantes.json` (**0 registros cada, com a causa escrita dentro**), `ferramentas/validar-banco.py` e `ferramentas/mutacoes-banco.py`. Corrigiu, de quebra, `ferramentas/impedancias.py` e a saída 2 da F1 na especificação. Todos com `publicar: false`. **Nada foi ao ar** — esta ilha não tem site.

## Os números desta ilha, e onde cada um é contado
- **18 constantes**, das quais **8 pendentes** — contadas em `dados/constantes.json`, nunca digitadas numa tela.
- **24 montagens** no domínio de saída da F1 — contadas por `ferramentas/impedancias.py`, que grava o total no próprio JSON.
- **73 afirmações** no portão da F1, 0 falha, **seis mutações deliberadas e seis reprovadas**. Reproduzir com `python3 ferramentas/impedancias.py --conferir` a partir de `ilhas/ohmetria/`.
- **0 registros no banco** e **0 itens esperando link de afiliado** — contados por `python3 ferramentas/validar-banco.py`, que imprime as duas contagens em toda passada, mais `itens sem saída de compra` (erro duro, hoje 0) e `itens intestáveis` (hoje 0). Nenhum desses números é digitado em lugar nenhum.
- **`vocabulario_sem_lastro`: 5 de 5** (0,5 · 1 · 2 · 4 · 8 Ω). É a dívida que o bloco 3 tornou contável: cada entrada do vocabulário de impedância de módulo sem um único módulo do banco por trás. **Enquanto ela não for zero, nenhuma frase desta ilha pode dizer que uma impedância "não existe no mercado".**
- **39 mutações do banco**, 39 decidiram certo — 6 mundos que têm de passar e 33 quebras que têm de reprovar, cada uma declarando a frase que espera ouvir. `python3 ferramentas/mutacoes-banco.py`.
- **144 estados de resposta da F1**, distribuídos em FECHA 37 · NÃO FECHA, SOBE 36 · NÃO FECHA, SÓ ABAIXO 17 · NÃO FECHA EM NENHUMA 30 · SEM PEDIDO 24 — contados por `python3 ferramentas/gerar-f1.py`, que imprime a distribuição em toda passada. **91 estados com piso de compra e 53 sem**, e os 53 dizem por quê.
- **8.262 afirmações** na bancada da F1, 0 falha (`python3 ferramentas/teste-f1.py`), e **41 mutações**, todas decidindo certo (`python3 ferramentas/mutacoes-f1.py`): 4 mundos que têm de passar, 28 quebras, 4 mortes, 4 sobre a prosa da especificação e o autoteste da própria bateria.

## O que está travando
- **Nada trava o próximo bloco de arquivo.** O bloco 3 está entregue; o que sobra sem depender de site nem de rede é pouco, e está dito abaixo.
- **A F1 está feita como regra e não tem página, e a página depende do 3b.** O bloco 4 escolheu (a) — construir com a lista de módulos vazia declarada na tela — e ela está construída, medida e gravada. O que falta é o snippet PHP, e ele **não** foi escrito de propósito: sem casca, um render de bancada serve menos do que o site serve, que é a cicatriz que a seção 8 do contrato mais cobra. Quem pegar o 3b escreve o PHP como TRADUTOR da referência e a bancada compara as duas.
- **O próximo passo desbloqueado que NÃO depende de site nem de rede é a F2 como regra**, pelo mesmo desenho da F1 — e ela tem uma diferença que muda tudo: a fórmula `formula-volume-caixa-selada` está `aguarda_validacao` e a `faixa-qtc-alvo` está `pendente`, então a F2 **não pode publicar litragem** enquanto a leitura não abrir. O que ela pode fazer hoje é o que a F1 fez com o banco vazio: o domínio de entrada, as recusas, o que ela se recusa a afirmar, e a trava de faixa de Thiele-Small, que é constante `publicavel`.
- **Trava o 3b em diante**: a casca precisa de WordPress, e a verificação no ar precisa do domínio na lista de rede.
- **Trava a CARGA do banco (3c) e trava a F3 inteira, e agora está medido pelas duas portas:** o egresso da nuvem recusa todo domínio de terceiro — `curl` em `www.lojadesomautomotivo.com.br` no bloco 1 e **WebFetch em `sac.taramps.com.br` às 19h30Z do bloco 2**, os dois com `EGRESS_BLOCKED`. A busca web funciona e resolve levantamento e **atribuição**; leitura de documento, não.
- **A dívida nomeada que isso cria, e ela tem lista:** 8 das 18 constantes estão `pendente` só porque o documento não foi aberto. `dados/constantes.json` → `pendencias_de_leitura` traz os quatro documentos, em ordem de quanto cada um desbloqueia, com **o que transcrever de cada um**. Quem tiver egresso aberto promove cada constante em uma linha — não é bloco, é leitura.
- **A F3 não nasce antes das prioridades 1 e 3 dessa lista**, e isso não é atraso: coincide com a ordem obrigatória F1 → F2 → F3 do `PROMPT.md`.
- **Uma pendência que não é de rede e não tem fonte conhecida:** `queda-de-tensao-maxima-aceitavel` foi **procurada e não encontrada** em fonte nenhuma. As páginas medidas dizem que o comprimento influencia a queda e param. Sem esse critério a F3 calcula a queda e **não dá veredito sobre ela** — dar veredito sem critério publicado é exatamente o que a ilha acusa nos concorrentes.
