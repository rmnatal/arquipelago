# CONSERTOS DA SENTINELA — ilha Robometria

Trava 19.4(c) do `ARQUIPELAGO.md`: todo conserto que uma ronda faz entra aqui, e a **ronda SEGUINTE abre esta lista antes de qualquer outra coisa** e reconfere no ar o que está escrito. É essa releitura que substitui a validação por quem construiu.
Uma linha por conserto: data, URL, o que mudou, e quem conferiu depois.

| data | URL | o que mudou | conferido pela ronda de |
|---|---|---|---|
| 2026-09-14 | — | **Nenhum conserto.** A ronda de 14/09/2026 (14h32Z) achou quatro coisas e as quatro caem na lista fechada 19.2, que vale mais que a 19.1: (1) a camada de cache da hospedagem serve HTML de 11/09 a cliente que não negocia `gzip`, nas 9 URLs — camada de hospedagem, e escolher entre purgar, desligar a variante, mandar `Vary` ou baixar o `max-age` é escolha entre duas opções defensáveis; (2) a R1 serve 63 pares no parágrafo de promessa e 73 na tabela, os dois vindos de código, com os kits abrindo uma linha por peça; (3) a frase da situação-âncora `liso\|nao` em `dados/r2-referencia.json` diz "as fontes divergem" e nomeia o Mundo Conectado nos dois lados, além de estar quebrada em português — dado técnico do banco; (4) os 32 itens publicáveis sem `url_busca` e sem `url_produto` dependem da sessão logada do painel da Shopee (25.6), que é do Raphael. As três primeiras foram para o `DESPACHO DA SENTINELA — 14/09/2026` no `PROMPT.md`; a quarta está em "Precisa do Raphael" no `dados/PAINEL.md`. **Não há nada nesta tabela para a próxima ronda reconferir — o que ela reconfere é aquele despacho, no ar, pelos "pronto quando" que ele declara.** | — |
| 2026-09-16 | — | **Nenhum conserto.** A ronda de 16/09/2026 (14h50Z) achou quatro coisas e as quatro caem na lista fechada 19.2: (1) quatro itens publicáveis (`wap-escova-direita-w300`, `wap-escova-esquerda-w300`, `wap-escova-frontal-wsmart`, `positivo-11206519`) com o botão de compra apontando para busca da Shopee de ZERO resultado — link morto é dado do banco, e a troca exigiria gerar link de afiliado novo, proibido à ronda diária pela seção 12; (2) a R1 publica "71 pares … cobrindo 30 dos 38 modelos", e um dos 71 aponta para `multi-ho401`, que é `nao_publicavel` — contagem de código, e escolher o universo é escolha entre duas opções defensáveis; (3) em 9 das 16 buscas vivas de peça o primeiro resultado não é a peça pedida — qualidade de palavra-chave, mesmo motivo do (1); (4) `dateModified` dos dois artigos serve 13/09 com a página mudada em 16/09 — o campo é derivado de `gerado_em` dos `*-fatos.json`, e decidir qual campo o alimenta é escolha entre duas opções defensáveis. Os quatro foram para o `DESPACHO DA SENTINELA — 16/09/2026` no `PROMPT.md`. **RECONFERÊNCIA DA RONDA ANTERIOR (19.4c): nada nesta tabela pedia reconferência — a ronda de 14/09 também não consertou nada. O que esta ronda reconferiu foi o despacho de 14/09, no ar, e os itens 1, 2 e 3 dele passaram e foram apagados do `PROMPT.md` pela 18.4.** | — |

## 14/09/2026 — primeira ronda desta ilha a registrar aqui

`dados/consertos.md` não existia na robometria: a ronda de 11/09/2026 também não consertou nada (os dois defeitos dela eram código de snippet) e não criou o arquivo. Ele nasce aqui, com zero conserto, para que a regra 19.4(c) passe a ter onde morar nesta ilha.
