---
ilha: aquametria
estado: viva
prioridade: 2
ultima_execucao: 2026-09-11T17:38Z
executando_desde: 2026-09-11T19:19Z
bloco_atual: "A ARVORE DA SECAO 16 CHEGOU AS TREZE PAGINAS NO AR (casca 1.5.0, manifest revisao 43, /status conferido). Fecha o que o despacho do Raphael de 11/09 deixou de pe depois do bloco da voz, e fecha SEM criar uma URL — que era a condicao para caber antes de 16/09, porque as oito paginas de nivel 1 e 2 seguem travadas por duas regras independentes. (1) TRILHA em doze das treze (a home nao tem, 16.3), com o nivel 2 em TEXTO enquanto a categoria nao existe: no dia em que a pagina nascer ela vira link sozinha, porque quem resolve o endereco e url_se_existir(). (2) BREADCRUMBLIST em JSON-LD levando SO os degraus com endereco de verdade — a trilha na tela mostra quatro degraus numa calculadora e o schema publica tres, de proposito: ListItem do meio sem `item` invalida a lista inteira para o Google, e lista invalida e lista ignorada, entao o schema 'mais completo' publicaria MENOS com cara de publicar mais. (3) VEJA TAMBEM com 2 a 4 irmas DERIVADAS do mesmo registro que alimenta o hub (irma digitada a mao envelheceria no dia da proxima calculadora), com afinidade declarada — mesma categoria primeiro — e a frase que linka a mae com a contagem CONTADA. A BANCADA MEDIA METADE DA PAGINA e por isso nasceu render-pagina-completa.php: dos tres renderizadores que havia, um montava so as quatro paginas da casca e os outros dois montavam corpo sem cabecalho e sem H1 — e a trilha nasce justamente entre o cabecalho e o H1. Medir a arvore em qualquer um dos tres seria medir o que nao existe, a cicatriz que a Robometria pagou tres vezes. O QUE AS MUTACOES ACHARAM, e uma era buraco de verdade no portao: com cinco calculadoras no ar NENHUMA pagina chega a ter cinco irmas candidatas, entao trocar o teto de 4 por 5 nao mudava nada do que o site serve e o portao ficava verde nas duas versoes — grade que nao pisa na borda, so que a borda nao existia no mundo. A saida foi a bancada FABRICAR a borda (modo `todas`, poe C2, C7 e C8 no ar) e ai o teto tem o que cortar. Outras tres mutacoes passaram por serem INERTES — a que tirava a trava do render_block nao duplicava nada porque o bloco so renderiza uma vez, e a que tirava is_front_page nao punha trilha na home porque o slug da home nao esta na arvore — e as tres foram reescritas ate morder. VERIFICACAO: teste-arvore 288 afirmacoes, 14 de 14 mutacoes reprovadas, 224 medicoes em Chromium nas 13 paginas em 360/390/781/782/783/1200 com 0 px de rolagem, e NO AR as 17h37Z: 13 de 13 em 200, 175 afirmacoes medidas no HTML servido, zero &#038; dentro de <script> nas treze, trilha sempre antes do H1 e uma so por pagina, nenhum degrau apontando para pagina inexistente. teste-voz, teste-seo-tecnico 177, conferir-slugs e conferir-protecao-funcoes sem falha; php -l limpo nos 10 snippets. 39 dos 78 produtos esperam link de afiliado (nao mudou; este bloco nao tocou em catalogo). ACHADO REGISTRADO, fora do escopo: /sobre/ e a unica das treze que nenhum CORPO de outra pagina cita — ela vive do menu e do rodape, que estao em todas, entao nao e orfa pelo 16.4(f), mas e a unica sem citacao editorial. E assunto de pauta (secao 17), nao de casca. Proximo: a pauta.md da secao 17 ainda nao existe nesta pasta, entao o proximo bloco sem URL nova e a reescrita na voz das paginas de conteudo/, que o despacho da voz deixou marcada como pendente ('as de conteudo/ ainda nao foram'). Tudo que cria URL espera a leitura de 16/09."
ultima_ronda: 2026-09-10T14:55Z
bloqueada_por: null
---

# ESTADO da ilha Aquametria — cópia da memória para sessões SEM ferramenta de memória

Gerado em 06/09/2026 a partir de /areas/projeto-aquametria.md e /areas/aquametria-corpus-buscas.md.
Regra: se a sessão TEM ferramenta de memória, a memória manda e este arquivo é só espelho. Se NÃO tem, este arquivo é o estado — leia-o, trabalhe, e atualize-o junto com REGISTRO.md no fim.
Credenciais NÃO ficam neste arquivo (repositório pode virar público). A Application Password do WordPress está só na memória.

## Identidade
- Nicho: aquarismo — calculadoras de dimensionamento e banco de dados técnico. 1º colocado da Bússola.
- Marca: Aquametria. Site: https://aquametria.com.br (WordPress 7.1 pt-BR, HTTPS ok desde 06/09/2026). Admin: aquametria_gestor.
- Nome institucional: "-metria" (ciência de medir). O Raphael NÃO aparece: sem rosto, vídeo, fórum, tráfego pago ou link pago. Autoridade = metodologia + procedência + widget instalado em lojas.
- Visual: instrumento de precisão; símbolo = recipiente graduado com linha de enchimento e escala. Sem peixe, bolha ou rosto.
- Cores: tinta #0D1B22 · lâmina #0E7C8C · papel #F4F7F7 · superfície #FFFFFF · traço #DDE5E6 · legenda #5C7075 · alerta #B5762A. Tipografia: Chivo (display), IBM Plex Sans (texto), IBM Plex Mono (dado/unidade). Implementadas no snippet da casca desde 07/09/2026 — reaproveitar as variáveis CSS `--aqm-*` em vez de repetir hex.

## Stack e infra (feito em 06/09/2026)
- HostGator Plano M, servidor br604, nginx na frente + mu-plugin "Caching" da HostGator (cache de página) → provavelmente sem plugin de cache extra.
- Plugin Code Snippets 3.10.2 ativo (REST /wp-json/code-snippets/v1/snippets). NUNCA WPCode. Sem construtor de página, sem plugin de SEO (sai por snippet), sem Wordfence.
- REST + Application Password funciona (header Authorization não é cortado). MAS o container da nuvem NÃO alcança o site por HTTP (proxy de saída bloqueia) — só GitHub. Descoberto em 07/09: `WebSearch` FUNCIONA na sessão da nuvem, mas `WebFetch` é bloqueado por domínio (seachem.com devolveu EGRESS_BLOCKED) — então número de fabricante colhido só por busca entra com status `fabricante-via-busca`, a reconfirmar. Por isso o desenho é PULL: a nuvem grava neste repositório; o snippet "Aquametria Sync" no site puxa manifest.json + arquivos do raw.githubusercontent.com e aplica só itens com publicar=true, conferindo sha256.
- Monetização: Shopee Afiliados aberto. Amazon Associados SÓ quando houver tráfego (regra 3 vendas/180 dias). Ticket típico R$380, comissão ~R$42.
- **LACUNA DO ID DE AFILIADO: RESOLVIDA PARA A SHOPEE (07/09/2026, 6º disparo).** 10 links curtos gravados no banco, no campo `afiliado` de cada produto, com `sub_id_1=aquametria` e `sub_id_2` = o código da calculadora (C3, C5, C12, C15) — é o `sub_id_2` que dirá no painel QUAL calculadora vende. Continuam SEM link 4 produtos (SunSun HW-303B, Atman AT-3338, Atman AT-3338S, Eheim Jäger 200 W): não têm anúncio do produto na Shopee, só peças de reposição. Produto sem link não entra no bloco de produto — a regra V15 do validador executa isso.

## Blocos
- BLOCO 1 CONCLUÍDO (04/09): 480 consultas paramétricas em 15 clusters (abaixo).
- BLOCO 2 CONCLUÍDO (07/09): 8 calculadoras especificadas em `dados/especificacao-calculadoras.md`, com o registro de constantes em `dados/constantes-calculadoras.json` (42 entradas, cada uma com fonte, data e status). As 8: C1 litragem (núcleo), C2 peso e carga no piso, C3 vazão/turnover, C5 aquecedor por delta térmico, C7 consumo e custo, C8 lotação, C12 mídia filtrante, C15 iluminação. Confirmado: C10 é banco de fichas, não calculadora; C14 foi absorvida pela C12; C13 (dosagem) NÃO entra no lote inicial (risco letal + só 2 dosagens verificadas no fabricante + o volume superestimado da C1 erra para overdose).
- Regras que o Bloco 2 fixou e que valem para os próximos: (a) toda constante tem id no JSON e status; constante `pendente` é PROIBIDA em fórmula publicada; (b) toda resposta é faixa com critério e constante em cada extremo, mais permalink citável; (c) quando as fontes divergem, a resposta publica a divergência e a atribuição, nunca a média; (d) onde falta fonte, a página explica por que não publica número — é isso que diferencia a Aquametria dos blogs.
- BLOCO 3 CONCLUÍDO (07/09, 2º disparo): modelo do banco de produtos em `dados/modelo-banco-produtos.md`, contrato formal em `dados/esquema-produtos.json`, dados em `dados/produtos-filtro.json` (5), `produtos-aquecedor.json` (4), `produtos-iluminacao.json` (4), `produtos-midia.json` (2) e `produtos-cotacoes.json` (0, vazio de propósito), com o validador `ferramentas/validar-produtos.py` (regras V1 a V14; hoje 15 produtos, 0 erros, 3 avisos). Decisões que valem para os próximos blocos: (a) procedência é por CAMPO, cada `fontes[]` diz quais campos sustenta; (b) variante de produto é registro próprio (Atman AT-3338 x AT-3338S muda 4 campos); (c) preço saiu do produto e virou série temporal com loja e data — nada entra sem data de leitura; (d) voltagem é requisito de SUGESTÃO (C3, C5, C15), não da ficha; (e) escada de 6 níveis de fonte, e em empate de nível o campo vira null; (f) derivado (turnover, W/L, lm/W, mL/L) nunca é gravado, só calculado.
- Achados do Bloco 3, com fonte nos registros: turnover implícito declarado pelos próprios fabricantes vai de 1,76 x/h (Eheim 2213) a 10,0 (Atman AT-3338S no piso da faixa) — os fabricantes divergem entre si por 5,7 vezes; eficiência de filtro de 34,3 a 166,7 L/h por W; a linha Roxin HT-1300/Q3 declara 0,67, 1,0 e 0,86 W/L nos três tamanhos (e deixa um vão sem modelo entre 200 e 250 L); 9 dos 13 equipamentos elétricos não declaram voltagem, então hoje a C5 e a C15 sugeririam ZERO produtos; 3 das 4 luminárias declaram lúmen (83 a 106 lm/W); Seachem Matrix (>700 m²/L) e Eheim SUBSTRAT pro (~450 m²/L) não publicam método de medição.
- BLOCO 3b CONCLUÍDO (07/09, 3º disparo): CASCA DO SITE em `snippets/aquametria-casca.php` — nome no Code Snippets "Aquametria Casca — identidade e estrutura do site", escopo global, ativo, `publicar: true` no manifest (revisão 3). Ele injeta paleta e tipografia sobre o tema ativo (inclusive redefinindo `--wp--preset--color--*` e `--wp--preset--font-family--*`, que é o que recolore um tema de blocos sem editá-lo), troca `core/site-title`/`core/site-logo` pelo logotipo SVG (recipiente graduado + wordmark Chivo) e `core/navigation` pelo menu Calculadoras · Metodologia · Sobre, cria as páginas `inicio`/`calculadoras`/`metodologia`/`sobre` com `show_on_front=page`, manda "Hello world!" e "Sample Page" para a LIXEIRA e imprime o rodapé com a tagline. Idempotente (2ª passada = 0 inserções), verificado em `php -l` e em teste de fumaça com WordPress simulado por stubs.
- CORREÇÃO 07/09 (5º disparo), casca **v1.0.2**: em tema de blocos o rodapé é a template part `footer` (`core/template-part` com `attrs['slug'] === 'footer'`), renderizada DENTRO do fluxo do conteúdo — imprimir o rodapé próprio em `wp_footer` deixa DOIS rodapés empilhados, o do tema (com "Criado com WordPress") acima do nosso. **Regra para sempre:** rodapé, cabeçalho e qualquer parte do tema se trocam pelo filtro `render_block` (substituição), nunca por hook de impressão (empilhamento). A casca agora devolve `aquametria_casca_rodape_html()` no lugar da template part `footer`/`rodape`; o `wp_footer` virou rede de segurança que só imprime se `aquametria_casca_rodape_impresso()` for falso; e o CSS tem cinto de segurança com `:has()` escondendo o crédito do WordPress e a template part de rodapé que não seja a nossa. `core/site-tagline` também passou a ser substituído por vazio (era por onde a tagline padrão do WordPress aparecia).
- **DESEMBARQUE APROVADO em 07/09/2026** (decisão do Raphael: "pode rodar sem a minha aprovação; se eu não gostar de algo peço pra alterar"). `desembarque.aprovado = true` no manifest. Calculadora, página-âncora, artigo e snippet de site saem com `publicar: true` sem consultar ninguém. SÓ continua com `publicar: false` esperando aprovação: conteúdo programático em escala (dezenas de páginas de cauda longa de uma vez).
- Duas regras novas que a casca criou: (a) **toda calculadora publicada precisa entrar no hub** — lista `aquametria_casca_calculadoras()` no snippet da casca, com `estado` em `em-construcao` ou `publicada`, mais o filtro `aquametria_calculadoras`; publicar calculadora sem virar o estado dela deixa a página órfã; (b) **conteúdo institucional mora em shortcode do snippet da casca** (`[aquametria_home]`, `[aquametria_calculadoras]`, `[aquametria_metodologia]`, `[aquametria_sobre]`), não em Markdown de `conteudo/` — assim editar o snippet edita a página, sem passar pelo editor do WordPress. `conteudo/` continua sendo o lugar dos artigos-âncora e das páginas de calculadora.
- TAREFA EXTRAORDINÁRIA 07/09 (6º disparo): links de afiliado no banco (acima), esquema v2 com o campo `afiliado` documentado + regra V15, `dados/produtos-cotacoes.json` deixou de estar vazio (10 cotações do painel, todas datadas), e `dados/afiliados-sem-produto.json` criado para link gerado sem ficha técnica. Mudanças de banco na mesma passada: entrou `ista-i-401-45` (o anúncio da Shopee é da luminária de 45 cm, não da de 60 do banco — colar o link no registro errado faria a C15 prometer 3717 lm e entregar 810), entrou `chihiros-wrgb-ii-pro-60` (74 W, 6630 lm, cobre 60 a 80 cm) e saiu `aquario-projetado-x1-fr-60w` (sem marca, sem comprimento, sem anúncio real; registro transcrito no REGISTRO.md).
- **BLOCO 4 COMEÇOU (07/09, 6º disparo): C1 no ar** — `snippets/aquametria-calculadora-litragem.php` (shortcode `[aquametria_calculadora_litragem]`, escopo front-end) + `conteudo/calculadora-de-litragem.md`, manifest revisão 7, os dois com `publicar: true`. Decisões que valem para TODAS as próximas: (a) todo o cálculo é JS no navegador, porque o site está atrás de cache de página; (b) **a calculadora se anuncia no hub pelo filtro `aquametria_calculadoras`** — não se edita a casca nem se bumpa `AQUAMETRIA_CASCA_VERSAO`; desativar o snippet devolve o cartão para "em construção" sozinho; (c) a chave do estado compartilhado é `aquametria.aquario` (com ponto) e a especificação foi corrigida para ela; (d) vírgula decimal é entrada válida (campos `text` + `inputmode=decimal`); (e) litro sai inteiro a partir de 100 L, uma casa abaixo — precisão falsa mente sobre a incerteza. Verificação: `php -l`, `proteger-funcoes.php` idêntico, teste de fumaça com stubs e **teste em Chromium real com 9 casos, zero erro de console**.
- **Sync v1.1.1 (07/09)**: a primeira página em Markdown expôs dois defeitos — o front matter era impresso na página e a linha do shortcode saía dentro de `<p>` (div dentro de p é HTML inválido). Corrigidos, mais tabela de Markdown agora dentro de bloco que rola no celular.
- **BLOCO 4 (08/09, 7º disparo): C3 no ar, e com ela o PRIMEIRO BLOCO DE PRODUTO** — `snippets/aquametria-calculadora-vazao.php` (shortcode `[aquametria_calculadora_vazao]`, escopo front-end) + `conteudo/calculadora-de-vazao-do-filtro.md` + `conteudo/divulgacao-de-afiliados.md`, manifest revisão 8, todos com `publicar: true`. Decisões que valem para TODAS as próximas calculadoras com produto:
  - **O banco de produtos viaja DENTRO do snippet.** O site não lê o repositório em tempo de execução, então `ferramentas/gerar-catalogo-filtros.py` reescreve o trecho entre `CATALOGO-INICIO` e `CATALOGO-FIM` a partir de `dados/produtos-filtro.json`. **Mexeu no banco, rode o gerador** — as duas cópias não podem divergir. Cada calculadora com produto ganha um gerador equivalente.
  - **Regra V16 (nova, no esquema):** link de afiliado NÃO entra em critério de sugestão. A lista é montada e ordenada só por adequação técnica; produto apto sem link aparece igual, só sem botão de loja. Ordenar por comissão é defeito.
  - **Anatomia do bloco de produto:** nasce DENTRO da resposta, como consequência do cálculo; placa tipográfica de marca (não foto — não há imagem com licença no banco, o campo `imagem` foi aberto e está null); o número que fez o produto entrar (aqui, o turnover que ele entrega NAQUELE aquário); ficha com fonte, URL e data; botão com `rel="sponsored noopener" target="_blank"`; **aviso de comissão obrigatório e visível** dentro do bloco, mais link para a página de divulgação. Sem produto que atenda, o bloco NÃO aparece e a tela diz por quê.
  - **Preço não entra em página de calculadora.** Snippet fica meses no ar; preço envelhece na tela. A política está escrita em `conteudo/divulgacao-de-afiliados.md`.
  - **Obrigatoriedade de campo pode ser condicional ao tipo** (`obrigatorio_se_tipo` no esquema, lido pelo validador): `coluna_maxima_m` só faz sentido em canister e sump; hang-on ganhou `coluna_maxima_nao_se_aplica`. A regra V10 (eficiência L/h por W) também ficou restrita a canister e sump. Esquema na versão 3.
  - **Sync 1.1.2:** o conversor de Markdown passou a entender citação em bloco (`>`), com estilo `.aqm-citacao` na casca 1.0.3.
  - **Nenhuma página nasce órfã:** casca 1.0.3 linka a divulgação no rodapé de todo o site; C1 1.0.1 (snippet e página) passou a LINKAR a C3.
  - Verificação: `php -l` e `proteger-funcoes.php` nos 4 snippets, validador de produtos com 0 erro, fumaça com stubs, **12 casos em Chromium real com zero erro de console** (inclusive os atributos do link de afiliado e a mescla do `localStorage` que não apaga o que a C1 gravou), conversão pelo próprio Sync e sha256 conferido.

- **BLOCO 4 (08/09, 8º disparo): C5 no ar, PAREADA COM O ARTIGO-ÂNCORA** — `snippets/aquametria-calculadora-aquecedor.php` (shortcode `[aquametria_calculadora_aquecedor]`) + `conteudo/calculadora-de-potencia-do-aquecedor.md` + `conteudo/quantos-watts-de-aquecedor-para-aquario.md`, manifest revisão 9, todos com `publicar: true`. É a primeira execução com o pareamento calculadora + artigo que o Raphael autorizou.
  - **A coleta veio antes da calculadora, e era o bloqueio real.** Os 4 aquecedores do banco estavam barrados por `voltagem` (nos quatro) e `faixa_ajuste_C` (nos três Roxin). A coleta de 08/09 resolveu os dois e ampliou o banco de 4 para 9 registros (Eheim Jäger 50/100/150/200 W e Roxin HT-1300/Q3 25/50/100/200/300 W). Resultado: **9 de 9 aptos** a serem sugeridos pela C5, contra 0 de 4 antes.
  - **Voltagem: `["110","220"]` nas duas linhas.** O que a fonte de varejo sustenta não é que um anúncio específico seja de uma delas, e sim que EXISTE versão para as duas tomadas. Por isso o cartão de produto obriga o aviso de conferir a voltagem no anúncio, e diz qual versão o link que conferimos abria.
  - **REGRA V17 (nova, no esquema v3): interseção conservadora.** Quando duas fontes do MESMO nível discordam sobre um intervalo, publica-se a interseção (maior dos mínimos, menor dos máximos) em `conflitos[].valor_conservador`, com `derivacao` obrigatória. Não é média nem escolha de fonte: é a única faixa que todas as fontes concordam que o aparelho cobre. A linha Roxin (22-34 °C contra 16-32 °C) virou 22-32 °C, e com isso o aquecedor mais vendido do Brasil voltou a poder ser sugerido — antes o campo virava null e a linha inteira saía por excesso de escrúpulo. **A calculadora que usar o valor conservador tem de dizer na tela que a faixa é a conservadora e por quê** (selo + frase da derivação no cartão). Interseção vazia continua sendo `campo-vira-null`. O validador confere a interseção de verdade; `minimo_para_sugerir` aceita a alternativa `conflito:campo`.
  - **REGRA V18 (nova): aquecedor sugerido tem de alcançar a temperatura-alvo** dentro da faixa que o registro sustenta. Duas barreiras de segurança vêm ANTES da adequação técnica no bloco de produto da C5: a voltagem da tomada e o termostato chegar ao alvo. Quem for barrado aparece com o motivo escrito na tela.
  - **Ordem do filtro de produto importa:** a potência é conferida PRIMEIRO, e só quem serviria pela potência entra na lista de "fora da lista" com explicação. Testar antes fazia a tela dizer "tem potência compatível" para modelo que nunca esteve na faixa.
  - **O que a C5 recusa fazer, e está escrito na página:** não publica a via física `P = U · A · ΔT` (falta `u-vidro-aquario`, pendente); não corrige por tampa (o campo muda o TEXTO, nunca o número — quantificar isso é a mesma constante pendente); não usa mínima por cidade (`temperatura-minima-por-cidade`, pendente — a mínima é entrada da pessoa, e é melhor assim); e **não extrapola regra de bolso acima de 10 °C de ΔT**, que é o limite que a única fonte com delta declarado (ReefFlow) afirmou. Acima disso a tela diz com essas palavras que nenhuma fonte cobre o caso e entrega a faixa genérica como PISO. Com ΔT ≤ 0 não devolve número nenhum.
  - **Achado do bloco, e o eixo do artigo:** a linha Eheim Jäger inteira dá **1,00 W/L cravado** no teto do volume declarado (25 W→25 L, 50→50, 75→75, 100→100, 125→125, 150→150). Ou seja, **o "1 W por litro" da web brasileira não veio de física: veio da prateleira.** E a mesma linha quebra a própria regra no 200 W (300-400 L = 0,50 W/L), enquanto o catálogo do fabricante declara esse 200 W para 30-400 L (13 vezes). Há ainda o mesmo Jäger de 150 W declarado para 125-150 L numa ficha e para 200-300 L noutra, as duas de varejo. Três conflitos ficam PUBLICADOS no banco e na tela, não escondidos.
  - **Constante `eheim-jager-linha-comercial` completada:** faltavam as potências de **125 W e 250 W** — eram justamente os dois dos "9 tamanhos" que a coleta do Bloco 2 não achara. Ganhou também `volume_declarado_por_potencia_L`, que é o que revela o 1,00 W/L da série.
  - **Correção na C1 (v1.0.2), que já era dívida:** `guardar()` SUBSTITUÍA o estado compartilhado em vez de mesclar — voltar à C1 para corrigir uma medida zerava a vazão que a C3 tinha calculado e o clima que a C5 tinha perguntado. Agora mescla, e `recuperar()` aceita estado sem `medidas` (que é como a C3 e a C5 gravam).
  - **Sync v1.1.3:** o conversor entende bloco de código cercado por crases triplas (a fórmula do artigo) e passa a converter **link relativo à raiz** — antes só link `https` virava `<a>`, e link relativo saía com os colchetes impressos no texto. Defeito silencioso: não é erro de sintaxe, só aparece lendo a página no ar. As páginas do repositório usam URL absoluta por convenção; o conversor agora aceita as duas.
  - **Ferramentas novas:** `gerar-catalogo-aquecedores.py` (a cópia do banco que viaja dentro do snippet da C5), `render-para-teste.php` (monta qualquer shortcode numa página HTML solta, com WordPress falso, e carrega todos os snippets menos o Sync — também testa a convivência entre calculadoras) e `teste-navegador-c5.mjs` (15 cenários em Chromium real).
  - Verificação: `php -l` e `proteger-funcoes.php` nos 5 snippets, validador com 0 erro (1 aviso conhecido, o V11 do Jäger 200 W, que é conteúdo da página), fumaça com stubs, **15 casos em Chromium real com zero erro de console**, conversão pelo próprio Sync nas 5 páginas e sha256 conferido contra os arquivos finais.

- **BLOCO 4 (08/09, 9º disparo): C12 no ar, PAREADA COM O ARTIGO-ÂNCORA** — `snippets/aquametria-calculadora-midia.php` (shortcode `[aquametria_calculadora_midia]`) + `conteudo/calculadora-de-midia-filtrante.md` + `conteudo/quanta-midia-biologica-o-aquario-precisa.md`, manifest revisão 10, todos com `publicar: true`. É o **vácuo de conteúdo nº 1** do Bloco 1, e o vazio que a própria página "Sobre" cita como motivo de a Aquametria existir.
  - **A coleta veio antes da calculadora, e de novo era o bloqueio real.** O banco tinha só DUAS âncoras de dosagem, as duas da Seachem — o que é duelo interno, não comparação. A coleta de 08/09 levou de 2 para 4 as dosagens declaradas por fabricante e de 2 para 6 os registros de mídia. Constantes novas: `jbl-micromec-dosagem` (5,00 mL/L — 650 g para 200 L, e a mesma embalagem é vendida como 1 L), `ocean-tech-bio-glass-dosagem` (12,50 mL/L — 1 L para cada 80 L), `seachem-matrixcarbon-dosagem` (0,625 mL/L) e `seachem-purigen-dosagem` (0,25 mL/L), mais `seachem-tidal-55-midia` (1,2 L para 200 L = 6,0 mL/L de mídia TOTAL). Constantes: 42 → 50; produtos: 21 → 25.
  - **O ACHADO, e o eixo do artigo: o paradoxo da área.** Área declarada × dosagem declarada = área entregue por litro de ÁGUA — e ela vai de **0,88 m² (Seachem) a 7,5 (JBL) a 18,8 (Ocean Tech): 21 vezes**. Pior: a marca que declara MAIS área por litro de mídia (Ocean Tech, 1.500 m²/L) é a que pede DEZ VEZES mais mídia que a que declara menos (Seachem, >700 m²/L). Se a área fosse o critério, a relação seria a inversa. Nenhuma das quatro publica método de medição. Não achamos ninguém, em português, cruzando essas duas colunas.
  - **Segundo achado: a Seachem discorda de si mesma em dois níveis.** Como fabricante de mídia diz 1,25 mL/L de Matrix; como fabricante de filtro põe 6,0 mL/L de espaço de mídia no Tidal 55. E dentro da própria copy do Matrix, 1,25 contra 2,64 mL/L. A **Ocean Tech é a única marca BRASILEIRA** que publica o número.
  - **DUAS FAMÍLIAS DE ÂNCORAS, em tabelas separadas.** Quem vende mídia declara dosagem biológica (1,25 · 2,64 · 5,00 · 12,50 mL/L). Quem vende filtro declara mídia TOTAL, que é volume de filtragem ÷ volume atendido (Tidal 55: 6,0; Eheim 2213: 12,0; Atman AT-3338: 3,6 a 10,7; AT-3338S: 8,8 a 26,3). Não se comparam de igual para igual, e a tela diz isso. O divisor é sempre o MAIOR volume declarado, que é a leitura menos favorável ao fabricante.
  - **Ficha ambígua vira conteúdo, não vira chute.** "3 cestos de 17x17x6 com capacidade de 1,6 litros de mídia" pode ser 1,6 L no total ou por cesto (4,8 L), e a geometria (1,73 L brutos por cesto) apoia a leitura por cesto. No AT-3338S a MESMA frase dá 3,5 L em cestos de 21×21×7 cm, que têm 3,09 L brutos e **não comportam** 3,5 L: ali só a leitura do conjunto fecha. Registrado como `conflitos[]` com `publicar-os-dois`; a tela publica as duas leituras e diz que não desempata.
  - **A saída que ninguém publica: o TETO FÍSICO.** Escolhido o filtro, a C12 mostra quanto do volume útil de mídia cada dosagem ocuparia só com a camada biológica, e avisa quando estoura ("o problema não é a mídia, é o filtro"). É conta trivial e não a encontramos publicada em português.
  - **As âncoras NÃO são digitadas no JavaScript:** saem do catálogo embutido, que sai do banco. Mídia nova com dosagem declarada entra na tabela sozinha no próximo `gerar-catalogo-midias.py` — que escreve DOIS blocos no mesmo snippet, porque a C12 consome os dois bancos (mídias e filtros).
  - **O que a C12 RECUSA fazer, escrito na tela:** não escolhe dosagem; não reparte o cesto em porcentagens (`proporcao-entre-camadas-do-cesto`, pendente — publica a ORDEM, que tem fonte); não converte grama em mililitro no carvão (`densidade-aparente-carvao-ativado`, pendente — mas a FREQUÊNCIA compara sem conversão: a regra BR manda trocar de 4 a 10 vezes mais que o fabricante do carvão); não compara marcas por área; não dimensiona mídia sem dosagem declarada (o Eheim Substrat pro sai no cartão sem número de compra); não estima validade de mídia biológica.
  - **A crítica de fundo, registrada como constante pendente `taxa-de-nitrificacao-por-area`:** as quatro dosagens são por litro de ÁGUA, mas o trabalho da mídia depende da amônia que entra — ou seja, da carga de peixes, que NENHUMA declaração pergunta. Todas dimensionam um processo biológico pela variável errada. É o que fecharia a C12 (e ajudaria a C8).
  - **Nenhuma página nasce órfã:** C1 (v1.0.3), C3 (v1.0.2) e C5 (v1.0.1) passaram a linkar a C12; casca v1.0.4 com o resumo do hub reescrito de "duas âncoras" para as quatro reais.
  - Verificação: `php -l` e `proteger-funcoes.php` nos 6 snippets, validador com 0 erro (1 aviso conhecido), fumaça estrutural (45 ids sem duplicata, tags balanceadas), **19 cenários em Chromium real com zero erro de console**, o teste da C5 rodado de novo sem regressão, conversão pelo próprio Sync nas 7 páginas e sha256 conferido (30 itens, 0 divergências).

- **BLOCO 4 (08/09, 10º disparo): C15 no ar, PAREADA COM O ARTIGO-ÂNCORA** — `snippets/aquametria-calculadora-iluminacao.php` (shortcode `[aquametria_calculadora_iluminacao]`) + `conteudo/calculadora-de-iluminacao.md` + `conteudo/quantos-lumens-por-litro-aquario-plantado.md`, manifest revisão 11, todos com `publicar: true`. Maior cluster empatado do Bloco 1 (83 consultas).
  - **A coleta veio antes da calculadora pela quarta vez seguida.** Dos 5 registros de iluminação, 4 estavam barrados. A coleta de 08/09 (toda por resultado de busca — `EGRESS_BLOCKED` em todas as lojas BR) trouxe voltagem bivolt e cobertura declarada para `ista-il-401-60` (56 a 66 cm) e `aquarios-do-rio-led-60cm` (55 a 75 cm), e acrescentou `wfish-wf-h600-wrgb`. Resultado: **3 aptas de 6**, 1 com link. **Duas luminárias que TÊM link de afiliado seguem barradas** (Chihiros sem voltagem, SunSun sem lúmen) — e a lista dos barrados virou conteúdo publicado na tela, com o motivo e a frase "link não promove produto barrado".
  - **O ACHADO, e o eixo do artigo: a régua usa a unidade errada.** O lúmen é ponderado pela visão humana, que pesa o verde e desconta azul e vermelho — as duas faixas da clorofila. Logo, a luminária feita para planta tende a marcar MENOS lúmens que uma calha branca de mesmo consumo, e a régua de lm/L a considera pior. O banco é coerente (Chihiros WRGB 89,6 lm/W contra Ista branca 106 lm/W), e o artigo apresenta isso como **indício, não demonstração**.
  - **A trilha do PPFD termina antes do Brasil:** no fórum de suporte da própria Chihiros — marca cujo argumento de venda é PAR — a resposta oficial é que **não existe teste de PAR oficial** da linha WRGB II Pro e que o usuário procure no YouTube. `ppfd-por-litragem` segue `pendente`, agora com URL e data, e a página publica o motivo em vez de só dizer "sem fonte".
  - **DECISÃO EDITORIAL DESTA EXECUÇÃO, que fecha a `pendencia_de_criterio` da entidade iluminação (aberta no Bloco 3): a Aquametria NÃO converte comprimento de peça em cobertura de aquário.** Nos 5 registros que declaram os dois números, o teto declarado vai de **1,15 a 1,59 vez** o comprimento da peça e o piso de 0,92 a 1,17 — não há razão constante a extrair. Virou a constante `cobertura-luminaria-declarada` (`convencao-editorial`, com `derivacao`), o `minimo_para_sugerir` da `c15-iluminacao` passou a exigir `comprimento_aquario_cm` declarado, e o esquema foi para a **versão 4**. A convenção só barra sugestão; nunca cria número.
  - **Segundo achado, registrado como conflito:** o número do nome não é o tamanho da peça. A Ista vendida como "60 cm" mede 56 cm na ficha de dimensões da MESMA página (`nome comercial` × `varejo`, tratamento `nivel-mais-alto-vence`), e o cartão da calculadora mostra os dois.
  - **Regra nova de arquitetura, pequena e permanente:** o banco é escrito **sem acento** e a tela sai **acentuada**, então o gerador manda a **estrutura** do conflito (campo, valor, origem) e a frase é escrita no JavaScript do snippet. Texto de banco não vai para a tela.
  - **O que a C15 recusa fazer, escrito na tela:** não publica PPFD (pendente, com o motivo a montante); não converte peça em cobertura; não publica número para marinho/coral (sem fonte); não publica preço nem tarifa de energia — o painel de consumo dá kWh/mês (física) e só converte em reais com a tarifa que o visitante digita.
  - **Nenhuma página nasce órfã:** C1 (v1.0.4), C3 (v1.0.3), C5 (v1.0.2) e C12 (v1.0.1) passaram a linkar a C15. A casca não foi tocada — a C15 se anuncia no hub pelo filtro e reescreve o próprio resumo.
  - Verificação: `php -l` e `proteger-funcoes.php` nos 7 snippets, validador com 26 produtos e 0 erro, **21 cenários em Chromium real com zero erro de console** (mais os testes da C5 e da C12 rodados de novo, sem regressão), conversão pelo próprio Sync nas duas páginas novas e sha256 conferido contra os arquivos finais.

- **CORREÇÃO EXTRAORDINÁRIA (08/09, 11º disparo): o Sync do site não se atualiza sozinho — e agora tem quem o atualize.** O Raphael abriu `/calculadora-de-litragem/` e viu o front matter YAML impresso dentro do corpo da página, com os `---` virados em travessão pelo `wptexturize`. O repositório estava certo (Sync v1.1.3, com o corte do front matter desde a v1.1.1); o **site** é que continuava na v1.1.0 colada à mão em 07/09. A causa é estrutural e vale para sempre:
  - **O Sync se pula a si mesmo de propósito.** Em `aquametria_sync_aplicar_snippet()` a checagem `$nome === AQUAMETRIA_SYNC_NOME_PROPRIO` devolve "pulado". Isso protege o site (o Sync não se reescreve no meio da própria execução) e, como efeito, **nenhuma correção no próprio Sync chega ao site por ele**. Quatro versões do conversor de Markdown ficaram só no repositório sem ninguém notar, porque o log dizia "aplicado com sucesso" — e dizia a verdade: as páginas foram aplicadas, pelo conversor velho.
  - **A saída é um snippet com OUTRO nome:** `snippets/aquametria-atualizador-sync.php`, "Aquametria Sync — atualizador do Sync". Nome diferente, então o próprio Sync o instala e ativa pelo caminho que já funciona; e a única coisa que ele faz é reescrever o Sync a partir do repositório. Ele confere sha256 contra o manifest, confere que o arquivo declara a versão que o manifest anuncia, faz checagem de sintaxe **sem executar** (`token_get_all` com `TOKEN_PARSE` levanta `ParseError`), salva o código atual em `aquametria_sync_backup_codigo` e **relê a option** antes de prosseguir, grava inativo e ativa depois, e então **verifica numa requisição NOVA** (`/wp-json/aquametria/v1/status`) se a versão nova responde — se não responder, **restaura o backup sozinho**. Loopback mudo conta como inconclusivo e mantém o código novo: derrubar por falta de prova seria pior. Roda no máximo uma vez por hora e só age quando a versão muda.
  - **Por que o campo `versao` entrou no manifest:** é o que o atualizador compara com a constante `AQUAMETRIA_SYNC_VERSAO` que está rodando. Obrigatório no item `aquametria-sync`.
  - **Reaplicação das páginas velhas:** depois de atualizar o Sync, o atualizador apaga o `sha256` registrado de cada item `conteudo:*` no estado do Sync e zera a revisão — é isso que obriga a reaplicação, já que o Sync pula arquivo cujo sha já foi aplicado. Ele **não** roda o sync na mesma requisição: ali quem está carregado é o conversor velho, que reescreveria o mesmo defeito.
  - **Sync v1.1.4:** o corte do front matter ficou tolerante a BOM, a quebra de linha antiga só com CR e a espaço/tabulação à direita do delimitador. Nenhuma dessas variações aparece no editor e qualquer uma devolvia o metadado impresso no corpo — a de espaço à direita produz exatamente a tela que o Raphael viu.
  - **Duas ferramentas de bancada novas:** `ferramentas/teste-conversor-markdown.php` (roda o conversor do próprio Sync sobre todos os arquivos de `conteudo/` e recusa resíduo de YAML; 17 casos) e `ferramentas/teste-atualizador-sync.php` (9 cenários do atualizador com WordPress e Code Snippets falsos, um processo por cenário — inclusive sha divergente, sintaxe quebrada e a restauração automática do backup).

- **BLOCO 4b COMEÇOU (09/09, 1ª execução do dia): expansão do banco, leva 1 — de 26 para 36 produtos.** Fila reordenada em 08/09 pôs o banco à frente de calculadora nova, porque é o banco que limita o tamanho legítimo do site e é ele que faz o bloco de produto aparecer. Entregue: 6 filtros novos (`atman-hf-0400`, `atman-hf-0600`, `atman-hf-0800`, `atman-at-3336`, `sunsun-hw-603b`, `sunsun-hw-302`) e 4 aquecedores (`atman-at-100`, `-150`, `-200`, `-300`), mais o fechamento da coluna máxima do `sunsun-hw-303b` (2,0 m, colhida no varejo especializado estrangeiro), que o destravou para a C3. Catálogo embutido: **C3 de 5 para 10 filtros, C5 de 9 para 13 aquecedores, C12 de 4 para 5 filtros com volume útil de mídia**. Manifest revisão 15. Correções da fila, medidas no `main`: o item 4b(a) (voltagem) já estava concluído desde 07 e 08/09 — sobrou só o Chihiros —, e a C5 já sugeria 9 aquecedores, não 0 de 4.
  - **Achado que vale dinheiro: ampliar o catálogo REDUZ links na tela no curto prazo.** Medido em Chromium, 100 L comunitário: a C3 antes mostrava 3 cartões com 2 links; agora mostra 5 cartões com 1, porque os produtos novos encaixam melhor tecnicamente e passam à frente dos que têm link. É a regra "ordem por adequação técnica, JAMAIS por comissão" funcionando — e a prova de que **gerar link virou o gargalo da receita**: 10 produtos esperam `afiliado.url` da Sentinela Estratégica.
  - **Regra nova de teste:** número de produto cravado em teste de navegador é armadilha — reprova exatamente o trabalho que a fila manda fazer. O teste da C5 passou a conferir a FAIXA da regra (de 3 a 5 cartões) e o da C12, que a tabela não murche nem repita linha.
  - **Nunca preenchido por falta de fonte:** voltagem do `sunsun-hw-302` e do `chihiros-wrgb-ii-pro-60`, e `volume_filtragem_L` dos hang-on e do AT-3336. Toda ficha nova é `transcrita-varejo`: nenhum manual foi lido direto, porque o egresso da nuvem bloqueia `WebFetch` em todos os domínios tentados.

- **BLOCO 4c LEVA 1b (09/09, disparo encadeado da noite): a tabela pré-renderizada da C3 e da C5 passou a dizer QUAL produto atende cada faixa.** Manifest revisão 20; C3 e C5 na versão 1.3.0. O 4c dessas duas já estava entregue na revisão 18 — o que faltava era a coluna de produto, que é a metade do 4e-B que cabe dentro do 4c: a pessoa passa a saber que sai dali com produto **antes de preencher qualquer campo**, e um modelo de linguagem passa a ter indicação de compra citável.
  - Cada uma das seis linhas traz o modelo, a **especificação que o fez entrar** ("650 L/h — 6,5 renovações/h nos 100 L"; "150 W — 110 ou 220 V"), a procedência e a data na mesma frase, e o link de loja quando existe (`rel="sponsored noopener"`, `target="_blank"`), com aviso de comissão próprio junto da tabela.
  - **O critério é o mesmo do script, e agora isso é verificável:** `escolher()` do JavaScript ganhou espelho em PHP (`aquametria_c3_produtos_exemplo`, `aquametria_c5_produtos_exemplo`). Comissão não ordena nada (V16).
  - **A linha do comprável, rotulada.** Quem atende melhor quase sempre não tem link — hoje 4 das 6 linhas da C3 e 5 das 6 da C5. Quando isso acontece, sai embaixo, escrito "Com link hoje, na mesma faixa", o **primeiro da mesma ordem** que tem. A ordem não muda; muda o que a tela conta.
  - Três ressalvas que a coluna obrigou e que são o conteúdo: **a tabela da C5 não conhece a voltagem da tomada nem a temperatura-alvo**, que são as duas barreiras de segurança do formulário (V18) — então publica a voltagem que a ficha declara, ou diz que não está confirmada, e manda conferir antes de comprar; a linha de 300 L avisa que a faixa passa do maior degrau e ali a resposta é mais de um aparelho; e quando o volume declarado pelo fabricante é menor que o do exemplo, a célula diz (acontece hoje com o Seachem Tidal 55, único com link na faixa de 300 L e declarado para 200 L).
  - FAQPage: C3 de 8 para **13** perguntas, C5 de 8 para **14** — entraram as de COMPRA, uma por volume, cada uma respondida com o mesmo modelo e o mesmo número que a tabela serve.
  - **Campo `imagem`, correção que evitou perder dado bom:** nenhuma das 21 URLs tinha sido anulada, mas faltava onde registrar a falta de conferência. Nasceram `imagem.verificado_em` e `imagem.motivo_sem_verificacao` (esquema versão 5 → 6) e a **regra V21** do validador, que exige o motivo em vez de aceitar a imagem anulada. A regra escrita, para a próxima sessão: **não conseguir BUSCAR um arquivo não é evidência de que a URL esteja errada** — o egresso da nuvem barra o CDN da Shopee, e quem confere se a foto carrega é a Sentinela Técnica, no Chrome.
  - Dois consertos no ferramental: o `teste-navegador-c5.mjs` quebrava por ambiguidade (dois avisos de comissão na página) e reprovava por **13 falhas de rede** que são só o egresso barrando as fontes do Google. Falha de rede agora sai contada à parte e o caso mede erro de **script**, que é o que ele existe para medir. Teste que reprova sempre treina a próxima sessão a ignorá-lo.
  - **NÃO CONFIRMADO NO AR:** o egresso continua bloqueando `aquametria.com.br`. Repositório na revisão 20, site na 11 desde 08/09 — **nove revisões paradas**. Precisa de alguém com navegador abrindo a URL do Sync com `&forcar=1`.

- **BLOCO 4c COMEÇOU (09/09, disparo extra das 10h25): C3 e C5 ganharam visibilidade em IA.** Manifest revisão 18. A Sentinela Técnica mediu naquela manhã **JSON-LD ZERO em 13 de 13 páginas** — a regra de primeira classe do Raphael, de 08/09, não tinha sido implementada em lugar nenhum. Entregue nas duas calculadoras que já vendem:
  - **(a) JSON-LD no `wp_head`**, um bloco por página com `@graph` de dois nós: `WebApplication` (com `applicationCategory`, `featureList` de 8 itens, `isAccessibleForFree`, `offers` a preço zero e `publisher`) e `FAQPage` com **8 perguntas cada**, toda resposta carregando número e fonte. **Sai no `wp_head` pelo MESMO motivo que o script sai no `wp_footer`:** o retorno do shortcode atravessa os filtros do `the_content`, que trocam `&` por `&#038;` e quebrariam o JSON tanto quanto quebraram o JavaScript em 08/09.
  - **(b) Tabela de exemplos pré-renderizada no HTML**, 30/60/100/150/200/300 L, resolvida em PHP no servidor. A C3 publica faixa comunitária, mira de carga média e faixa de plantado; a C5 publica os **dois cenários de frio** — até 10 °C, onde vale a ReefFlow, e acima disso, onde só sobram as regras sem condição declarada e a faixa vira PISO — com o degrau da linha comercial de cada um.
  - **(c) Resposta antes da explicação**, num bloco `-direta` que vem ANTES do formulário, com o número, o critério e a procedência (fonte nomeada + número + data) dentro da mesma frase.
  - **O conserto que o 4c obrigou, e que vale para as próximas três:** número que aparece na tabela servida E na calculadora não pode ser escrito duas vezes. As bandas de turnover da C3 e as regras de W/L mais a linha comercial da C5 saíram do JavaScript e passaram a nascer no PHP (`aquametria_c3_bandas`, `aquametria_c5_regras`, `aquametria_c5_linha_comercial`), viajando para o script como `AQM_C3_BANDAS`, `AQM_C5_REGRAS` e `AQM_C5_LINHA`. Conferido que o JSON emitido pelo PHP é **idêntico ao literal que estava no JS**. Nenhuma fórmula mudou.
  - **`ferramentas/teste-navegador-visibilidade-ia.mjs`:** o teste que faltava, e que agora mede o que a Sentinela mediu à mão. Abre a página em Chromium **com o JavaScript DESLIGADO** — é o que um crawler de IA recebe, e a única prova honesta de que o número está no HTML servido e não foi desenhado depois. **46 asserções, 46 passaram.** Inclui a asserção que fecha o círculo: o número da tabela servida para 100 L bate com o que a calculadora devolve para 100 L (C3: 180 a 1.000 L/h; C5: 100 a 150 W).
  - **`ferramentas/conferir-protecao-funcoes.py`:** existia só o `proteger-funcoes.php`, que REESCREVE o arquivo e por isso nunca serviu como verificação antes do commit. Agora há o que só confere: 8 snippets, 147 funções, todas dentro de `function_exists`.
  - Achado editorial que a tabela da C5 tornou visível: **a partir de 300 L a faixa passa do maior degrau da linha de referência (300 W)**, e a resposta honesta é mais de um aparelho — que também é o mais seguro por modo de falha do termostato. E o buraco de catálogo que a Sentinela apontou fica agora explícito na página: a 100 L o degrau comercial é 150 W, e nenhum dos aquecedores com link de afiliado é 150 W.
  - **NÃO CONFIRMADO NO AR:** o egresso da nuvem para `aquametria.com.br` continua bloqueado, então o Sync não foi acionado desta sessão e a revisão aplicada no site não pôde ser conferida contra a 18 do manifest. Ver o REGISTRO de 09/09.

- **BLOCO 4b LEVA 3 CONCLUÍDO (09/09, 4ª execução do dia): o banco de espécies foi de 12 para 25**, e todos os 25 ganharam `familia`. Manifest revisão 17, tudo com `publicar: false` — bloco de dados, sem desembarque, nenhum snippet tocado.
  - **Os 13 novos:** espada, molinésia, colisa, tricogaster, mato-grosso, rodóstomo, ramirezi, ancistrus, tetra-negro, coridora-panda, botia-palhaço, tetra-brilhante e oscar. **Frentes mínimas declaradas agora em nove degraus: 45, 60, 75, 80, 90, 100, 120, 150 e 180 cm** — é a malha de página de volume sem repetir número. Porte de 2,2 a 48 cm.
  - **REGRA E15, espelho não confere espelho.** Fechar a família do guppy trouxe a MESMA ficha da FishBase pelo outro domínio (`.se` em vez de `.org`): duas urls distintas, e o E14 antigo teria promovido o registro a conferido sem ninguém ter conferido nada. Agora o validador colapsa domínios do mesmo corpo (`fishbase.se` + `fishbase.org` = `fishbase`) antes de contar fontes, e o registro com duas urls e um corpo só sai com aviso e não conta — nem no E14 nem no `minimo_para_sugerir`. **Vale para todo banco da ilha: url diferente não é fonte diferente.**
  - **Emenda ao E10:** um registro pode ser `parcial` E ter conflitos declarados. Completude e divergência são ortogonais, `status_registro` carrega um campo só, e vale o mais restritivo — `parcial` barra a página, `conflito` não.
  - **`ferramentas/testar-validador-especies.py`:** os testes negativos viraram comando. Uma corrupção por regra, E1 a E15, cada uma exigindo que a regra certa reprove com o código certo, e o banco real conferido intacto no fim. **15 testes, 15 pegaram, 0 falha.** Regra que não roda vira decoração; teste que não roda, também.
  - **A taxonomia das fontes está em revisão e o banco NÃO uniformiza:** no mesmo dia e na mesma base, os *Paracheirodon*, o tetra-negro e o tetra-brilhante vieram como **Acestrorhamphidae** e o mato-grosso e o rodóstomo como **Characidae**. Três espécies já saem sob gênero novo (*Osteogaster aenea*, *Hoplisoma panda*, *Petitella rhodostoma*); o id segue o nome do comércio brasileiro e o novo vai para `sinonimos_cientificos`. **Este banco envelhece por revisão taxonômica, não por preço** — daí os 365 dias de revalidação contra os 180 do banco de produtos.
  - **Quatro registros barrados, nenhum número estimado:** molinésia (sem frente mínima), tricogaster (sem convivência), ancistrus (sem temperatura — as faixas das espécies irmãs apareceram na busca e NÃO foram copiadas; e o cascudo do comércio é *Ancistrus* sp. '3', espécie não descrita) e guppy (um corpo de fonte só). Fora da leva de propósito: camarão *Neocaridina* (compêndio não cobre invertebrado) e acará-disco (sem frente mínima atribuída).

- **BLOCO 4b(c) CONCLUÍDO (09/09, 3º disparo do dia): nasce o BANCO DE ESPÉCIES**, em `dados/especies-agua-doce.json` (12 espécies), com contrato próprio em `dados/esquema-especies.json` e validador próprio em `ferramentas/validar-especies.py` (regras E1 a E14). Manifest revisão 16, as três entradas com `publicar: false` — bloco de dados, sem desembarque, nenhum snippet tocado, nenhuma página do site alterada.
  - As 12: neon, cardinal, guppy, platy, betta, coridora bronze, paulistinha, rasbora arlequim, acará-bandeira, barbo-sumatra, kinguio e otocinclo. Porte de 2,2 a 48 cm; frentes mínimas declaradas de 45, 60, 80, 90 e 100 cm.
  - **Escada de fontes própria: não existe fabricante de peixe.** O topo é base científica (FishBase) e compêndio de aquarismo (Seriously Fish), e a tabela `dominio_por_campo` diz qual manda em qual campo — biologia com a base, manutenção com o compêndio. Sem ela, "nível mais alto vence" daria à FishBase a última palavra sobre tamanho de aquário, que não é o assunto dela.
  - **Em conflito de campo de BEM-ESTAR o conservador é o MAIOR** (`cardume_minimo`, `comprimento_minimo_aquario_cm`, `base_minima_cm`, `altura_minima_cm`) — o OPOSTO do banco de produtos, porque errar espaço para menos custa a vida do animal. Regra E9, executada pelo validador.
  - **Litro nunca se grava** (regra E7): a fonte declara frente e base em cm, e o litro é derivado pela calculadora com a altura da pessoa. O validador detecta litro que bate com `base × altura` para qualquer altura de 25 a 60 cm.
  - **Regra E13, temperatura que é tolerância:** a FishBase publica 0 a 41 °C para o kinguio, que é tolerância da espécie e não recomendação. Campo `temperatura_e_tolerancia`; faixa com amplitude > 20 °C sem ele é ERRO, e com ele o registro sai sozinho do `minimo_para_sugerir` da C5.
  - **Regra E14, fonte única não é registro completo:** o guppy — o peixe mais vendido do Brasil — **não tem ficha própria no Seriously Fish** (só as irmãs *P. wingei*, *P. sphenops*, *P. velifera*, *P. latipinna*), ficou com uma fonte só e por isso é `parcial` e não passa no portão de página. Fechar a 2ª fonte dele é tarefa da próxima leva.
  - **Regra de atribuição por busca, e é ela que separa colher de inventar:** número só entra quando a busca foi RESTRITA ao domínio da fonte (`allowed_domains`) ou quando o resumo atribui o número à fonte pelo nome. Número de busca aberta sem dono não entra. Confirmado hoje que o egresso bloqueia também `fishbase.se`, `fishbase.org`, `seriouslyfish.com` e `en.wikipedia.org` — nenhuma página foi lida direto, e o limite da técnica ficou publicado como conflito no cardinal: duas leituras da MESMA página devolveram 2,5 e 3,0 cm SL no mesmo dia.
  - **O achado editorial:** o banco contradiz a regra de bolso de "1 cm por litro" com fonte, em quatro registros — paulistinha (3,8 cm de peixe pedindo 90 × 30 cm de base, porque manda velocidade e não massa), acará-bandeira (única altura mínima declarada, 50 cm: 100 L em formato baixo não atende), coridora (peixe de fundo, limita a ÁREA e não o volume) e betta (a fonte declara 45 × 30 cm para UM macho, e o varejo vende em pote de 2 L). É o material dos artigos do eixo "quantos litros para X peixes".
  - **Deliberadamente vazios:** `dificuldade` (julgamento editorial, não fonte), `familia` (nenhuma busca devolveu com atribuição), `comportamento` (só 5 dos 12 têm frase de fonte — por isso NÃO é obrigatório; quem sustenta lotação é `convivencia`) e `volume_minimo_declarado_L`.
  - Verificação: validador com 12 espécies, 0 erro, 0 aviso, mais **12 testes negativos** com o banco corrompido de propósito, um por regra, todos reprovando com o código certo e o banco restaurado depois; `validar-produtos.py` com 36 produtos e 0 erro (banco de produtos intacto).

- **TAREFA EXTRAORDINÁRIA 09/09/2026 (disparo manual, ~10h50 BRT): LOTE DE BANCO com 21 anúncios colhidos à mão no painel da Shopee — link, preço e IMAGEM na mesma passada.** Manifest revisão 19. O banco foi de 36 para **56 produtos** e de 10 para **31 cotações**. Iluminação 6 → 16 (linha Soma WRGB S-200 a S-1200 inteira, de 20 a 130 cm, mais Chihiros A901 e A451M); aquecedor 13 → 23 (7 Maxxi, RS-50, Ocean Tech Warmer X-5 150 W, Sicce Scuba Contactless 150 W), e o Roxin Q3 50 W, que já era apto e estava sem link, ganhou link. Decisões que valem para os próximos blocos:
  - **O campo `imagem` deixou de ser placeholder** (esquema v4 → v5): subcampos `url`, `largura`, `altura`, `fonte`, `coletado_em`, `alt` e `motivo_sem_medida`, com vocabulário de fonte e regras de tela. **Imagem é dado COMERCIAL**, como preço e link: vem do anúncio, nunca entra em `fontes[]` e jamais sustenta campo técnico. Produto sem foto NÃO sai do resultado — cai na placa tipográfica. Regras novas no validador: **V19** (imagem exige https, fonte do vocabulário, data e `alt` de no mínimo 20 caracteres) e **V20** (aviso contando produto com link e sem foto — hoje 10).
  - **Largura e altura ficaram `null` com motivo**: o egresso da nuvem barra `down-bs-br.img.susercontent.com` como barra toda loja, então a Aquametria não mede a imagem e não chuta dimensão. O cartão reserva o espaço por CSS (`aspect-ratio: 1/1` + `object-fit: contain`), que não salta o layout qualquer que seja a proporção real. **Quem consegue conferir se as fotos carregam é a Sentinela Técnica, no Chrome.**
  - **A regra das duas camadas de procedência foi aplicada com consequência, e doeu:** potência, voltagem, volume e lúmen declarados NO ANÚNCIO não viraram dado técnico. Resultado — a Chihiros A451M entrou com **3.500 lm** (varejo) e não com os 3.850 do anúncio; as **8 Soma ficaram `parcial` por falta de `fluxo_lm`**, que nenhuma das nove lojas brasileiras conferidas publica, e a C15 não sugere nenhuma; **9 dos 10 aquecedores novos ficaram com `voltagem: null`** porque o varejo localizado publica 110 V e parte dos anúncios abre 220 V, e voltagem errada queima o aparelho.
  - **O que destravou, medido:** C5 foi de 3 para **5** aquecedores sugeríveis com link e **a janela de 110 a 150 W fechou** (Ocean Tech Warmer X-5 150 W: 20 a 34 °C, 29 cm, quartzo, 110 e 220 V, tudo de varejo especializado). C15 foi de 1 para **3** luminárias sugeríveis com link (Chihiros A901 com 8.200 lm e A451M com 3.500 lm).
  - **Conflito publicado em vez de resolvido:** o Warmer X-5 é vendido com três tetos de volume (100, 130 e 150 L). A ficha publica o mais baixo, que é a única afirmação que as três leituras sustentam, e `conflitos[]` guarda as três com atribuição.
  - Dois defeitos de tela consertados na mesma passada: o gerador da C5 imprimia `None a 100 L` quando o varejo declara teto sem piso (agora `até 100 L`), e a C15 imprimiria `cobre 90 a 90 cm` para a A901, que declara **um** comprimento e não uma faixa (agora `exatamente 90 cm`).

- **TAREFA EXTRAORDINÁRIA 09/09/2026 (disparo manual da noite): A DUPLA CONDIÇÃO — a C3 parou de recomendar filtro que ela mesma diz que não serve.** Manifest revisão 21, C3 na versão 1.4.0. O Raphael abriu a C3 no ar com o aquário dele (189,6 L, plantado, carga média) e viu três coisas; as três foram feitas nesta execução.
  - **O defeito era de credibilidade, não de cálculo.** O primeiro recomendado era o Atman HF-0600 e o cartão dele escrevia, logo abaixo do nome, que o fabricante declara o modelo para até 150 L e que "a declaração do fabricante não cobre o seu caso". A elegibilidade olhava só a VAZÃO. **Regra nova V22 do esquema, nos dois lados da C3 (PHP servido e JavaScript):** entra nos recomendados só quem passa nas DUAS condições — vazão dentro da faixa E volume do visitante dentro do volume declarado pelo fabricante, cortando nas duas pontas. Quem não declara volume entra, com a ressalva escrita. Quem declara e não cobre vai para uma **seção separada, abaixo, rotulada** "Atendem a vazão, mas o fabricante não cobre esse volume". Nunca misturado, nunca em primeiro lugar.
  - **A ordem virou três degraus, e V16 foi reescrita para isso:** elegibilidade dupla → adequação técnica → e só como DESEMPATE entre itens **tecnicamente equivalentes** quem tem link aparece antes. "Equivalente" precisa de número, senão o desempate come a ordem técnica: mesmo **décimo da largura da faixa**. É convenção editorial declarada, não constante de fabricante. Continua proibido comparar taxa de comissão em qualquer lugar, e produto sem link continua aparecendo — muda a POSIÇÃO de quem já era equivalente, não a presença de ninguém.
  - **O que o desempate não resolve, medido:** no caso do Raphael a faixa do plantado é estreita (334 a 948 L/h, décimo de 61) e nenhum par cai no mesmo degrau, então o topo continua sendo o Atman AT-3336, que não tem link. Isso é o desempate obedecendo, não falhando. **O que resolve é link para o AT-3336**, e isso é da Sentinela Estratégica. No mesmo volume em comunitário, a faixa vai a 1.896 L/h, cinco modelos caem no mesmo décimo e o topo virou quatro itens com link.
  - **Lote de 8 filtros + 3 imagens, colhido à mão no painel da Shopee.** Banco de 56 para **64 produtos**, cotações de 31 para **39**. As specs vinham do ANÚNCIO, e anúncio (nível 6) sustenta só existência, nomenclatura e preço — cada número foi reconferido em varejo BR especializado por busca. **Placar: 3 de 8 já vendem dentro do resultado** (Eheim classic 600/2217 em 127 V e 220 V, SunSun HW-702B 220 V — catálogo da C3 de 10 para 13 filtros, de 3 para 6 com link); **5 esperam UM ou DOIS campos** — 4 esperam só voltagem (HW-702A, XBL-600, CT-1000-3, que também quer a coluna), 2 esperam vazão com fonte (Maxxi Pro MP-600 e Aquaverso APK-600, que a busca não achou em fonte nenhuma acima de anúncio). Todos os 8 já têm foto e link no banco.
  - **Regra que ficou:** voltagem separa REGISTROS, não variantes de um registro — o Eheim 2217 está no banco duas vezes, e por isso o nome de tela passou a carregar a voltagem quando o registro declara uma só.
  - **`ferramentas/teste-navegador-c3-dupla-condicao.mjs`, arquivo novo, 16 casos, todos passaram:** executa o caso exato do Raphael em Chromium. Duas decisões para ele não mentir — roda um **segundo caso** em perfil comunitário, porque no plantado o desempate nunca dispararia e o teste passaria sem exercitar a regra; e **reprova** quando uma linha da tela não casa com ficha do catálogo, em vez de filtrar órfãos e passar em silêncio (quase aconteceu quando o nome ganhou a voltagem).

- **BLOCO 4d CONCLUÍDO (09/09, redisparo das 12h): a casca no celular ganhou menu, e o site ganhou ícone próprio.** Manifest revisão 22, casca na versão 1.3.0. Os dois itens no mesmo snippet, como o payload pediu — e entregues em parte porque o 4e inteiro não cabe numa execução (o disparo anterior, `cse_01R1msCiPtLH1iFvqdgmUfLT`, morreu em 6 minutos sem commitar nada; esta execução não achou rastro da causa, e não há branch nem PR pendente dela).
  - **Menu hambúrguer abaixo de 782 px** (a quebra do próprio WordPress), com `aria-expanded`, `aria-controls` de id contado (o `render_block` pode trocar mais de um `core/navigation` na mesma página), Escape fechando e devolvendo o foco ao botão, clique fora fechando, e reset ao alargar a janela — sem esse último o `aria-expanded` mentiria para o leitor de tela.
  - **A regra que governa o desenho é de indexação, não de layout:** os três links saem SEMPRE no HTML servido dentro de `<nav>`; quem esconde a lista é o seletor `.aqm-nav-caixa[data-aqm-menu]`, e esse atributo quem põe é o JavaScript do rodapé. **Sem JavaScript o menu não some** — volta a ser a fileira de links, que é o que o crawler de IA recebe. Esconder por padrão e contar com o script para revelar trocaria defeito de celular por defeito de visibilidade em IA.
  - **Ícone próprio:** `remove_action( 'wp_head', 'wp_site_icon', 99 )` mais SVG na aba, PNG de 32 px como `alternate icon`, `apple-touch-icon` de 180 px e `theme-color`, os três como data URI dentro do snippet — nada sobe para a biblioteca de mídia. O desenho é o recipiente graduado da marca, gerado por **`ferramentas/gerar-favicon.php`** entre os marcadores `FAVICON-INICIO`/`FAVICON-FIM`, pelo mesmo motivo que o catálogo é gerado dentro das calculadoras. **O SVG da aba tem canto arredondado e o PNG do iOS não**, porque o iOS aplica a própria máscara e arredondar por baixo dela vira borda dupla. Redução de paleta para 64 cores levou o PNG de 9.124 para 3.238 bytes sem mudar o que se vê.
  - **Ferramental novo, porque nenhum teste do projeto olhava para o cabeçalho:** `ferramentas/render-casca-para-teste.php` (monta a página como o tema de blocos monta, com o cabeçalho vindo do filtro `render_block`) e `ferramentas/teste-navegador-casca.mjs` (**31 asserções, 31 passaram**), que inclui o caso do crawler — a mesma página em celular **com o JavaScript desligado**, exigindo os três links visíveis. O `render-para-teste.php` ganhou `remove_action` de verdade e um `wp_site_icon` de mentira na prioridade do core: é ele que permite PROVAR que a casca tirou o ícone do WordPress do `wp_head`.
  - **NÃO CONFIRMADO NO AR:** o egresso continua bloqueando `aquametria.com.br`. Repositório na revisão 22, site na 11 desde 08/09 — **onze revisões paradas**. Precisa de alguém com navegador abrindo a URL do Sync com `&forcar=1`.

- **T3 LEVA DE FECHAMENTO DE FAIXA (09/09, execução da noite): o banco passou dos 300 W e ganhou cobertura acima de 80 cm.** Manifest revisão 27, C5 na 1.3.1, C15 na 1.1.2, banco de 68 para **78 produtos**. Primeira execução em que a fila não veio de julgamento e sim da varredura: `dados/cobertura-de-faixa.md` tinha deixado uma ordem de compra escrita, e esta execução atacou só os dois primeiros itens dela.
  - **Quatro aquecedores** (`hopar-j-226-400w`, `hopar-j-226-500w`, `oceantech-warmer-x-5-250w`, `oceantech-warmer-x-5-500w`) e **seis luminárias** da família Chihiros WRGB II (90, Slim 90, Pro 90, 120, Slim 120, Pro 120). Nenhum dos dez tem link de afiliado — entram sugeríveis pela adequação técnica, com cartão sem botão de loja (V16).
  - **A família WRGB II foi escolhida por declarar COBERTURA EM FAIXA, não por marca.** As duas Chihiros A-Series que já estavam no banco declaram 80 e 90 cm cravados, então 95, 100, 105 e 110 cm saíam sem nenhuma opção mesmo com elas lá. Registro que declara faixa cobre degrau; registro que declara um número cobre um ponto — e é isso que move a medida de cobertura.
  - **O achado que vale dinheiro: um campo colhido para um produto destravou outro.** A `chihiros-wrgb-ii-pro-60` estava no banco desde 07/09 COM link e barrada por `voltagem` só. Catalogando a família, o varejo BR especializado (AquaBetta) declarou dois membros diferentes da linha como bivolt no título, e a ficha da série descreve entrada de 100 a 240 V por fonte externa de 12 V. **A C15 foi de 3 para 4 luminárias sugeríveis com link sem que nada mudasse no produto.** Regra que fica: quando um campo barra vários registros, colher esse campo para a LINHA vale mais que colher dez produtos novos.
  - **Duas páginas no ar foram corrigidas na mesma passada**, porque afirmavam o que deixou de ser verdade: a calculadora de iluminação e o artigo de lúmens diziam que a Chihiros não é sugerida por falta de voltagem. Página que contradiz o próprio catálogo é defeito de julgamento, e não sobrevive a uma execução. A correção virou conteúdo melhor: o exemplo foi resolvido, e por dado que apareceu — nunca por exceção aberta para produto que rende comissão.
  - **EMENDA À V12, igual à do E10 no banco de espécies:** `parcial` vale junto com `conflitos[]` quando falta campo obrigatório, porque completude e divergência são ortogonais e `status_registro` carrega um campo só — vale o mais restritivo. O caso que obrigou: a `chihiros-wrgb-ii-pro-120` tem 7.700 lm/130 W na Fazenda Submersa e 11.170 lm/138 W na Green Aqua, empate de nível faz o campo virar null, e campo null deixa o registro parcial. **O conflito é a CAUSA da incompletude.** A V12 também passou a reprovar status `conflito` sem conflito declarado. Esquema na versão 7.
  - **Achado editorial: entre 300 W e 500 W o mercado brasileiro não tem degrau.** Só a Hopar J-226 publica 400 W. Como a janela da C5 a 310 L vai de 310 a 465 W, o aquário de 310 a 333 L tem exatamente UM aparelho único possível no país — e acima disso a resposta honesta continua sendo dois aquecedores, que a C5 já dizia na tela sem saber que descrevia uma lacuna de catálogo nacional.
  - **`ferramentas/atualizar-manifest.py`, arquivo novo, nascido de defeito medido:** na primeira execução achou QUATRO itens com sha vencido no manifest (`render-para-teste.php`, `teste-navegador-c5.mjs`, `-c12.mjs`, `-c15.mjs`), todos alterados em execuções anteriores sem ninguém atualizar o registro. Os quatro são `publicar: false` e o site não quebrou — mas o mesmo esquecimento num item publicável faz o Sync recusar o arquivo e a revisão do site ficar para trás em silêncio, que é o defeito da seção 4 do contrato. **O passo manual que a gente esquece é o passo que vira ferramenta.**
  - **`conferir-entidades.mjs` estava vermelho desde a manhã de 09/09 sem defeito de código:** passava o `<script type="application/ld+json">` por `node --check`, e JSON não é JavaScript. Agora cada bloco vai para o verificador da linguagem dele; o JSON-LD passa por `JSON.parse`, que é MAIS severo aqui (entidade escapada dentro de string quebra a análise). Falhas 3 → 0. Na mesma varredura, a casca 1.3.0 tinha voltado a escrever a entidade por extenso num comentário — trocada pela descrição em palavras, terceira ocorrência da mesma regra.
  - **Regra escrita de uma vez, na terceira vez que a ilha tropeça nela:** *teste que fixa tamanho de catálogo, ou que exige que um buraco continue aberto, reprova exatamente o trabalho que a fila manda fazer.* O `teste-navegador-c15.mjs` reprovou em 5 asserções e três delas eram o buraco que este bloco fechou. Contagem fixa virou piso; a asserção "Chihiros barrada por voltagem" caiu (media defeito de coleta nosso, não promessa da tela) e virou "nenhuma barrada sai sem motivo escrito" mais "a linha Soma inteira aparece"; e o caso "nenhum produto cobre 120 cm" virou **115 cm**, que é o buraco que sobrou porque o mercado declara 90 a 110 ou 120 a 140 e ninguém declara o meio.

- **BLOCO 4e / T8 — A VITRINE: FEITA NA C3 em 10/09/2026 (C3 v1.5.0, revisões 36 e 37).** Está no ar: carrossel `scroll-snap` em CSS puro sem biblioteca, cartão que é âncora de verdade quando há link e `div` com o lugar do botão reservado quando não há ("link de loja em breve", contrato 7), promessa acima da dobra, barra fixa no celular enquanto o resultado está fora da tela e rolagem até ele **só no envio explícito** do formulário. Mantidos sem exceção: `rel="sponsored"`, `target="_blank" rel="noopener"`, aviso visível de comissão, ordem por adequação técnica e nunca por comissão. **Preço passou a sair e sai sempre datado** — o proibido é preço cravado como atual, não preço com a data da coleta ao lado. Produto sem foto **não some**: espaço reservado neutro na posição que a técnica lhe deu. Portão: `ferramentas/teste-navegador-c3-vitrine.mjs`, 38 afirmações, com a ordem da vitrine obrigada a ser idêntica à da lista técnica. **E FEITA NA C5 em 10/09/2026 (C5 v1.5.0, revisão 38)**, com tudo isto mais duas coisas que só ela tem: a lista dela é partida em dois grupos (dentro da faixa calculada / o degrau comercial acima) e o cartão diz de qual grupo ele é, porque um trilho de `scroll-snap` não comporta cabeçalho de grupo — e um cartão do degrau acima dizendo "dentro dos ... W" reencenaria em foto e botão de loja a contradição consertada na 1.4.0; e a sequência é calculada UMA vez em `pintarProdutos()` e passada para `pintarVitrine()`, em vez de recalculada dos dois lados. Portão: `ferramentas/teste-navegador-c5-vitrine.mjs`, 44 afirmações. **FALTA na C15, na C12 e na C1** — e na C1 provavelmente não nasce, porque litragem é geometria e geometria não escolhe produto. Ao levar a vitrine a cada uma: reescrever as frases de preço daquela calculadora na MESMA versão, e acentuar o `alt` das imagens daquele banco, que é texto de tela. **E conte foto e link DEPOIS do portão de elegibilidade:** este arquivo dizia que a C5 tinha 11 dos 14 com link e foto, número medido no BANCO — no CATÁLOGO que ela publica são 2 fotos em 18, porque 9 dos 11 com foto não passam no `minimo_para_sugerir`.

- PRÓXIMO: **BLOCO 4c, leva 2 — C12 mídia e C15 iluminação**, com exatamente o mesmo desenho da leva 1 (resposta direta, tabela pré-renderizada de 30 a 300 L, JSON-LD `WebApplication` + `FAQPage`, procedência na frase, constante compartilhada nascendo no PHP). Depois a **leva 3: C1 litragem e os três artigos-âncora** (nos artigos, `FAQPage`; e a `Organization` com `sameAs` na home, que mora na casca). Ao publicar cada leva, acrescentar o caso novo em `ferramentas/teste-navegador-visibilidade-ia.mjs` — o teste é a única coisa que sobrevive à sessão.

- SUPERADO (era o próximo antes do disparo extra de 09/09 às 10h25): **BLOCO 4b, leva 3**, nesta ordem — (a) **ampliar o banco de espécies para 25 a 30 registros** (molinésia, espada, colisa, mato-grosso, rodóstomo, tricogaster, apistograma, ancistrus e camarão *Neocaridina*), fechando junto a 2ª fonte do guppy e a `familia` dos 12 atuais; (b) fechar `fluxo_lm` do `sunsun-ade-400c` e do `wfish-wf-h600-wrgb` e a voltagem do Chihiros, os três itens que barram a C15; (c) mais filtros e aquecedores de marcas ausentes (Ocean Tech, Hopar, Boyu, Sarlo Better), faixas de 200 a 400 L. Depois vem o **4c — retrofit de visibilidade em IA** nas cinco calculadoras publicadas. Só depois do 4b e do 4c voltam **C2 peso e carga no piso**, C7 consumo e C8 lotação. As travessas da C2 continuam registradas e valem quando ela chegar: não publica espessura de vidro nem veredito de "a laje aguenta", e a carga de projeto da NBR 6120 entra rotulada como `norma-via-secundaria`, com essas palavras na tela.

- SUPERADO (era o próximo em 09/09 de manhã): **BLOCO 4b, leva 2**, nesta ordem — (a) **iniciar o banco de espécies (4b(c))**, que destrava o eixo aberto "quantos litros para X peixes" e não depende de link de afiliado nenhum; (b) fechar `fluxo_lm` do `sunsun-ade-400c` e do `wfish-wf-h600-wrgb` e a voltagem do Chihiros — são os três únicos itens que barram a C15, hoje a calculadora publicada com o catálogo mais fraco; (c) mais filtros e aquecedores de marcas ausentes (Ocean Tech, Hopar, Boyu, Sarlo Better), com atenção às faixas de 200 a 400 L. Meta do bloco: pelo menos 60 itens, cada faixa de volume com opção. Depois vem o **4c — retrofit de visibilidade em IA** nas cinco calculadoras publicadas (tabela de exemplos pré-renderizada, resposta antes da explicação, JSON-LD, procedência na frase). Só depois do 4b e do 4c voltam as calculadoras que faltam: **C2 peso e carga no piso**, C7 consumo e C8 lotação. As travessas da C2 continuam registradas e valem quando ela chegar: não publica espessura de vidro nem veredito de "a laje aguenta" (falta tensão admissível e coeficiente de segurança citáveis, e o veredito é de engenheiro), e a carga de projeto da NBR 6120 entra rotulada como `norma-via-secundaria`, com essas palavras na tela.

- **T7 FECHADO NAS CALCULADORAS (10/09/2026): a C1 recebeu as três peças de visibilidade em IA e o retrofit foi de 4 para 5 de 5.** Snippet `aquametria-calculadora-litragem.php` v1.2.0, manifest revisão 32. Entraram a resposta antes da explicação, a tabela pré-renderizada de seis aquários e o JSON-LD (WebApplication + FAQPage de nove perguntas) no `wp_head`. Nenhuma linha de cálculo mudou. **A decisão de projeto que vale para a próxima ilha: o eixo da tabela pré-renderizada pertence à pergunta, não ao formato.** A C3, a C5 e a C12 indexam por litro; a C15 por centímetro de luminária; e a C1 por centímetro de aquário, porque nela **o litro é a saída** — quem abre a página tem a fita métrica na mão e não sabe o volume, então uma tabela indexada por litro responderia à pergunta que a pessoa ainda não consegue fazer. Também vale o precedente do bloco sem produto: litragem é geometria e geometria não escolhe produto, então a tabela não tem link de loja e **diz por quê** em vez de ficar em silêncio (seção 7 do contrato). Os 3 artigos fecharam no mesmo dia, na leva 4 abaixo.
- **T7 FECHADO POR INTEIRO (10/09/2026, leva 4): os 3 artigos-âncora ganharam a resposta direta e o JSON-LD.** Snippet novo `aquametria-artigos.php` v1.0.0, manifest revisão 33. Com isso o retrofit de visibilidade em IA cobre **8 de 8 páginas** de ferramenta e artigo — as 5 calculadoras desde a leva 3 e agora a C5, a C12 e a C15 do lado dos artigos. Medido em Chromium com o JavaScript DESLIGADO: **108 afirmações, zero falha.**
  - **Por que um snippet separado, e não mais um bloco na casca.** Artigo não tem shortcode de ferramenta, então não dá para a página se reconhecer por `has_shortcode()`, que é como cada calculadora se descobre. Aqui a página se reconhece pelo **slug** — o mesmo do front matter e do manifest. Vale para toda ilha: página de conteúdo se identifica pelo endereço, página de ferramenta pelo shortcode que ela carrega.
  - **A regra que este bloco recusou obedecer ao pé da letra, e por quê: tabela pré-renderizada não se inventa.** Os três artigos já serviam tabela própria no HTML — a linha comercial Eheim Jäger, as quatro dosagens de mídia, as três réguas de lm/L —, escritas em Markdown e convertidas pelo Sync. Acrescentar uma tabela nossa por cima seria decoração, e o portão da seção 5 do contrato é **servir resposta citável**, não servir uma tabela. O que o teste passou a exigir é que a tabela que o artigo JÁ TINHA continue no HTML servido, com cabeçalho e pelo menos três linhas e com número em toda ela.
  - **A resposta direta de artigo é outra coisa que a de calculadora.** A da calculadora resolve um caso de exemplo; a do artigo tem de entregar a TESE com número, porque é ela que vai ser citada fora de contexto. As três saíram assim: no aquecedor, que a única fonte com condição declarada sustenta de 1,0 a 1,5 W/L para até 10 °C e que o "1 W por litro" veio da prateleira (1,00 W/L em seis degraus da linha Eheim Jäger); na mídia, que as quatro dosagens vão de 1,25 a 12,50 mL/L e a área entregue por litro de água varia 21 vezes na direção contrária ao argumento de venda; na iluminação, que "baixa" é 10, 15 ou 20 lm/L conforme a fonte e que o lúmen desconta justamente as faixas da clorofila.
  - **`ferramentas/teste-navegador-artigos.mjs`, arquivo novo — e as duas afirmações que valem o arquivo são de COERÊNCIA, não de estado:** *todo número da resposta direta existe no corpo do artigo* e *toda resposta do FAQPage carrega ao menos um número que o corpo sustenta*. Nenhum número esperado fica gravado no teste. Corrigir um dado com fonte melhor não reprova nada — desde que o topo e o FAQ mudem junto, que é exatamente o defeito que ele existe para pegar. É o desenho do `teste-navegador-c1-tabela.mjs` aplicado a texto em vez de formulário, e a resposta à regra que a ilha já escreveu três vezes: *afirme a promessa, nunca o estado*.
  - **Controle negativo conferido, nos dois sentidos:** a página como estava antes deste bloco (sem JSON-LD e sem o bloco do topo) dá **15 falhas**; e trocar um único número da resposta direta por um que o artigo não sustenta é reprovado **pelo valor** ("sem lastro: 1,7"). O teste já pegou um defeito real antes do commit: uma resposta do FAQ da C12 sobre troca parcial de mídia não carregava número nenhum, e a saída certa não foi afrouxar a regra — foi amarrar a resposta às quatro dosagens que a página publica.
  - **`ferramentas/render-artigo-para-teste.php`, arquivo novo:** monta o artigo como o site monta — Markdown pelo conversor do PRÓPRIO Sync, shortcodes executados, o escape de E-comercial dos filtros do `the_content` aplicado por cima, e então `wp_head`/`wp_footer`. Sem ele o teste mediria o HTML que a gente imaginou, não o que o site serve. O `render-para-teste.php` ganhou uma linha para isso: o slug da página de teste virou global (`__slug_pagina`), porque sem poder trocá-lo não havia como renderizar artigo nenhum.
  - **`citation` com data em toda fonte** entrou no nó `Article` e virou afirmação do teste. Procedência declarada no schema é o que separa esta ilha de uma fazenda de conteúdo para quem lê a página por máquina — e é barato: as fontes já estavam no front matter de cada artigo.
  - **NÃO CONFIRMADO NO AR:** o egresso da nuvem para `aquametria.com.br` continua bloqueado (`EGRESS_BLOCKED` no `/status`, com `?v=` novo). Repositório na revisão 33, site na 11 desde 08/09 — **vinte e duas revisões paradas**.
- Depois do Bloco 4: Bloco 5 (lista de prospecção do widget) — a única alavanca de link do projeto.
- Entregas vão como arquivos em ilhas/aquametria/ (dados/, conteudo/, snippets/, ferramentas/) + item no manifest.json (publicar=false até o desembarque). Antes de commitar mudança no banco de produtos, rodar `python3 ferramentas/validar-produtos.py` na pasta da ilha.
- Regra de entrega fixada pelo Raphael em 07/09/2026: o bloco só conta como entregue quando está no `main`. Push direto em `main` funciona neste repositório (foi assim que o Bloco 2 entrou).
- Dívida técnica anotada: `dados/constantes-calculadoras.json` (Bloco 2) está sem acentuação; os arquivos do Bloco 3 já saem acentuados no texto de tela. Normalizar antes do desembarque.

## Regras de gravação de snippet (fase 4b do playbook — lições pagas na Real 21)
- Code Snippets moderno é app React; o editor do DOM não é fonte da verdade. Gravação sempre por REST: POST /wp-json/code-snippets/v1/snippets/{id}.
- Nunca confie na ausência de erro: releia o snippet depois de gravar, confira code.length e code_error === null. (DELETE de snippet por REST já devolveu 200 sem apagar.)
- ModSecurity mata gravação em SILÊNCIO: evitar a superglobal $_SERVER literal (usar add_query_arg(array())) e "User-agent:" escrito por extenso.
- Snippet começa com /** — o plugin põe o <?php. Todo PHP passa em php -l; JS embutido em node --check.
- Backup antes de editar: link action=clone (nasce INATIVO, sufixo [CLONE]). Dois ativos com as mesmas funções = fatal.
- Texto para tela sai acentuado (UTF-8). Nome de snippet descritivo e óbvio, nunca numerado.
- **O snippet Sync NÃO se atualiza sozinho, por desenho.** Toda correção no próprio Sync chega ao site pelo snippet "Aquametria Sync — atualizador do Sync", nunca por colagem manual do Raphael. Mexeu em `snippets/aquametria-sync.php`? Suba a versão no cabeçalho E o campo `versao` do item `aquametria-sync` no manifest — sem isso o atualizador não vê motivo para agir e a correção fica parada no repositório.
- **Depois de publicar qualquer página vinda de `conteudo/`, buscar a URL no ar e conferir que o corpo começa pelo TEXTO, não por metadado.** Nunca confie no "aplicado com sucesso" do log: em 08/09/2026 o log estava certo e a página estava errada, e quem viu o defeito foi o Raphael, não a rotina. Se a sessão não alcançar o site (egresso bloqueado), registrar a URL a conferir em vez de dar por concluída.

### O defeito de 08/09/2026 — as cinco calculadoras estavam mortas no ar

Diagnosticado pelo Raphael, com o console do navegador aberto. Três regras
saíram daqui, e **nenhuma delas é negociável**:

- **JS e CSS de shortcode nunca vão no retorno do shortcode — vão no `wp_footer`
  (comportamento) e no `wp_head` (estilo), senão o WordPress escapa os `&` e
  quebra o script inteiro.** O que volta do shortcode ainda atravessa os filtros
  de texto do conteúdo, que trocam cada `&` por `&#038;`. O primeiro `&&` do
  script vira `&#038;&#038;`, o navegador para com `SyntaxError` e a calculadora
  inteira morre: o formulário não calcula, a resposta fica com a classe
  `-oculto` para sempre e **o bloco de produto com os links de afiliado nunca
  aparece**. As cinco páginas carregavam bem e não calculavam nada — 58
  ocorrências de `&#038;` na C1, 50 na C3, 48 na C12, 57 na C15. Clicar em
  "Calcular" só fazia submit nativo do formulário. Corrigir trocando `&&` por
  `if` aninhado é gambiarra: a causa é o LUGAR do script, não o operador.
- **Calculadora só conta como entregue depois que alguém EXECUTOU o cálculo e
  viu o resultado e os links na tela.** Página que carrega não é calculadora que
  funciona. Sem alcance ao site, o mínimo é rodar
  `ferramentas/teste-navegador-cinco.mjs`, que clica em Calcular num Chromium de
  verdade e confere resultado e links de afiliado.
- **Todo slug publicado é conferido contra o link que o hub publica.** O hub
  apontava `/calculadora-de-aquecedor/` para a C5, cuja página é
  `/calculadora-de-potencia-do-aquecedor/`: 404 no ar.
  `python3 ferramentas/conferir-slugs.py` compara front matter, manifest,
  constantes `AQUAMETRIA_C*_SLUG`, o catálogo do hub na casca e todo link de raiz
  escrito em snippet ou conteúdo.

O que ficou de rede permanente (os dois testes têm controle negativo conferido —
reprovam o código defeituoso e aprovam o corrigido):

| ferramenta | o que garante |
| --- | --- |
| `ferramentas/teste-escape-shortcode.php` | retorno do shortcode sem `<script>` e sem `<style>`; zero `&#038;` dentro de script ou style; `&&` intacto; estilo antes do HTML, script depois |
| `ferramentas/teste-navegador-cinco.mjs` | as cinco calculam num Chromium de verdade; resposta sai do oculto; sem submit nativo; link de afiliado `sponsored` + `noopener` + `_blank` e aviso de comissão |
| `ferramentas/conferir-slugs.py` | nenhum endereço publicado aponta para página inexistente |

`ferramentas/render-para-teste.php` foi refeito para não mentir: até 08/09/2026
ele tratava `add_action` como no-op e não aplicava o escape de `&`, então o
script saía inline e o teste passava enquanto o site estava quebrado. Agora ele
monta a página como o WordPress monta — `wp_head`, conteúdo já escapado,
`wp_footer`.

O WordPress também **renomeia `post_name` em silêncio** quando o endereço já está
ocupado, e o Sync respondia "ok" do mesmo jeito. Desde a v1.1.5 o Sync devolve o
permalink real e marca `slug_ok:false` no estado; a casca (v1.1.0) resolve o
endereço de cada página pelo `_aquametria_id`, não por slug adivinhado, e **só
publica link se a página existir** — sem página, fica o selo "Em construção".

### A regra do ponto de partida — escrita em 08/09/2026, no segundo erro do mesmo dia

**Ao corrigir um snippet, o ponto de partida é SEMPRE o código-fonte do
repositório, nunca o HTML servido pelo site nem uma cópia dele — o HTML servido
pode conter entidades escapadas que, copiadas de volta para o fonte, corrompem o
código de forma permanente e silenciosa.**

Como isso aconteceu: a correção da manhã mudou o lugar do script (do retorno do
shortcode para o `wp_footer`) e ficou certa. Mas ao reescrever o arquivo, parte
do JavaScript veio do HTML que o site estava servindo, e esse HTML já trazia os
`&&` escapados. As entidades entraram no arquivo do repositório e ficaram lá,
convivendo com os `&&` sãos — no mesmo bloco de script, alguns operadores certos
e outros corrompidos. O sintoma no navegador é idêntico ao do defeito original
(`SyntaxError: Invalid or unexpected token`), o que faz perder tempo procurando
no lugar errado.

Três coisas ficam desta:

- **Entidade numérica (`&#NNN;`) não tem uso legítimo nenhum em `snippets/`.**
  Texto acentuado vai em UTF-8 direto. Qualquer ocorrência é corrupção. Esta é a
  regra dura que `ferramentas/conferir-entidades.mjs` aplica.
- **Nunca faça busca-e-troca cega de entidade.** As entidades NOMEADAS da função
  `esc()` das calculadoras — `.replace(/&/g, '&amp;')` e as três irmãs — são
  código correto. Trocá-las pelo caractere quebra o escape de HTML das cinco
  calculadoras de uma vez. O verificador separa as duas famílias justamente por
  isso: proíbe a numérica em toda parte e trata a nomeada por lista de exceção.
- **Entidade em COMENTÁRIO também sai.** Os comentários das cinco calculadoras
  explicavam o defeito escrevendo a entidade por extenso. Isso é inofensivo para
  o PHP e péssimo para o diagnóstico: um `grep` de segurança acusa o comentário,
  quem lê conclui que o fonte está corrompido e "conserta" o que estava são. Os
  comentários passaram a descrever a entidade em palavras.

| ferramenta | o que garante |
| --- | --- |
| `ferramentas/conferir-entidades.mjs` | zero entidade numérica no fonte dos snippets; zero entidade dentro de `<script>`/`<style>` renderizado (menos a cadeia de `esc()`); `node --check` em cada bloco de script; script depois do conteúdo |
| `ferramentas/teste-apelidos.php` | cada apelido de endereço resolve para o slug canônico, endereço fora do mapa não resolve, e todo destino existe em `conteudo/` |

Controle negativo conferido: corrompendo um `&&` dentro de `aquametria_c1_js()`,
o verificador reprova nos três níveis (fonte, bloco renderizado e `node --check`)
e reproduz o `SyntaxError` exato que o Raphael viu.

### Endereço adivinhado não pode dar 404 (08/09/2026)

O Raphael pediu `/calculadora-de-aquecedor-de-aquario/` e levou 404. Não era slug
trocado: a página da C5 está em `/calculadora-de-potencia-do-aquecedor/` e o hub
aponta certo. O endereço pedido é o que qualquer pessoa adivinha a partir do nome
da calculadora. A casca v1.2.0 tem 22 apelidos que redirecionam **301** para a
página canônica, e só quando ela existe publicada — redirecionar para outro 404
seria pior que o 404 original. Calculadora nova entra com os apelidos dela no
mesmo commit.

### Code Snippets 3.10 — o que a API mudou (diagnosticado em 07/09/2026, depois de o site cair com "Há um erro crítico")
Na versão instalada no site (3.10.2), a API PHP do plugin não é a antiga:
- A classe do modelo é `Code_Snippets\Model\Snippet`. `new \Code_Snippets\Snippet()` é **fatal** — detecte a classe com `class_exists()` antes de instanciar.
- `\Code_Snippets\save_snippet()` devolve o **objeto Snippet**, não o id. `$id = save_snippet(...)` vira 0/erro — leia `$ret->id`.
- `save_snippet()` de um snippet marcado **ativo** roda `test_snippet_code()`, que faz `eval` do código na mesma requisição. Grave **inativo** e chame `activate_snippet()` depois.
- O validador (`Code_Snippets\Utils\Validator`) recusa com "Cannot redeclare function X" quando o snippet já está carregado naquela requisição.

**Regra que vale para SEMPRE, em todo snippet da ilha:** cada função de nível
superior tem de estar envolvida em `if ( ! function_exists( 'nome' ) ) { ... }`.
Sem isso, atualizar um snippet ativo falha ou o deixa inativo em silêncio. Vale
para a casca e para todas as calculadoras futuras. A ferramenta
`ferramentas/proteger-funcoes.php` faz isso mecanicamente:
`php ferramentas/proteger-funcoes.php entrada.php saida.php` (ela não entra no
manifest — é ferramenta de bancada, não código do site).

## Portão (desembarque)
- Publicar em escala nunca é autônomo. A Fundação deixa pronto (publicar=false) e para. Página programática só existe com dado suficiente para ser útil sozinha.

---

# CORPUS DO BLOCO 1 — 480 consultas em 15 clusters (levantado em 04/09/2026)
Fontes: ~100 buscas no Google e ~70 páginas BR (Aquariofilia.Net, AquaPeixes, Brasil Reef, AquaOnline, Aquarismo Paulista, Aquário Vivo, Peixes e Aquarismo, Aquários Sobrinho, Escola de Aquário, Grupo Sarlo, Cobasi, Petz, fabricantes). [obs] = fraseado literal colhido; [var] = variação sobre padrão observado.

## Os 15 clusters (nº de consultas)
- C1 Litragem e volume útil (25) — "aquário de {C}x{L}x{A} quantos litros", inverso litros→medidas, desconto de substrato, bruta x real
- C2 Peso do aquário cheio e espessura do vidro (11) — "quanto pesa aquário de {N} litros", "laje/móvel aguenta", "espessura para {N} litros"
- C3 Vazão, turnover e dimensionamento de filtro (65) — "melhor filtro para aquário de {N} litros" é o padrão mais replicado da web BR; inverso "filtro de {X} l/h serve para quantos litros"; "{modelo} até quantos litros"
- C4 Circulação, sump e bomba de retorno (13) — nicho marinho
- C5 Potência do aquecedor e delta térmico (34) — "quantos watts para {N} litros", "{n} W por litro", ambiente {Ta}°C → alvo {Tw}°C, região/estação
- C6 Resfriamento (9) — cooler, chiller, verão
- C7 Consumo elétrico e custo mensal (11) — {W}w × {h}/dia × tarifa
- C8 Lotação, bioload e aquário mínimo (57) — "quantos {espécie} em {N} litros" é a maior cauda longa do nicho
- C9 Cardume mínimo e proporção macho/fêmea (7)
- C10 Compatibilidade par a par e comunitário (83) — "{espécie A} pode viver com {espécie B}", "parâmetros ideais para {espécie}"
- C11 Ciclagem: cronograma, dose de amônia, diagnóstico (30)
- C12 Mídia filtrante: volume, ordem, troca (14)
- C13 Dosagem: condicionador, sal, medicamento, KH, fertilizante (32)
- C14 TPA: percentual ↔ litros, frequência, tolerância térmica (6)
- C15 Iluminação, fotoperíodo, CO2, substrato, algas (83)

## Decisões de arquitetura que saíram do levantamento
- Calculadora devolve FAIXA com critério de cada extremo e a fonte. As fontes BR divergem em quase todo cluster (turnover 3x a 10x; lm/L 10-20 vs 20 para o mesmo "baixa"; sal grosso com 3 valores em 5 fontes). Mostrar a divergência É a metodologia.
- C1 é o núcleo: litragem alimenta 8 dos 15 clusters → ESTADO DE AQUÁRIO compartilhado, não 15 formulários isolados.
- C10 é BANCO: fichas de espécie normalizadas (temperatura, pH, GH, tamanho adulto, cardume mínimo, agressividade, zona de nado) + interseção de faixas. Melhor cauda longa e candidato natural a widget de loja.
- Modelos de equipamento aparecem nas buscas (Sunsun HW-303B, Atman AT-3338/HF-800, Seachem Tidal 35/55, Roxin Q3/Q5, Hopar J-226/3028, Boyu FEF-230, Eheim Classic 2213/Jager). O banco de produtos responde metade das consultas.

## Três vácuos de conteúdo confirmados
1. Quantidade de MÍDIA BIOLÓGICA por litragem/vazão (C12) — nenhuma fonte BR publica número. **PREENCHIDO em 08/09/2026**: a C12 foi ao ar com QUATRO dosagens de fabricante (Seachem 1,25 e 2,64; JBL 5,00; Ocean Tech 12,50 mL/L), 10x entre o piso e o teto, mais o teto físico do cesto. A Ocean Tech é a única marca brasileira que publica o número.
2. Potência de aquecedor por DELTA TÉRMICO real (C5) — todos repetem "1 W/L"; fontes: 1 W/L genérico, 1,3 (eHow), 2 no Sul (Casa da Ada), 1-1,5 até 10°C de delta (ReefFlow). Calculadora que pede a mínima da cidade é inédita no BR.
3. DUREZA DA ÁGUA DA TORNEIRA por cidade brasileira (C13) — exige coleta própria.
- Menor: CICLO DE TRABALHO do aquecedor (C7) — fontes citam 8h/dia (Seu Pet Saudável) e 10h/dia (Grupo Sarlo), todos multiplicam por 24h.

## Procedência das constantes de DOSAGEM (C13 — risco letal)
- VERIFICADAS no fabricante: Mbreda Micronutri 1 ml/10 L brutos semanal (mbreda.com.br); Veromar Teste KH — amostra 5 mL, 1 gota = 0,5 °dKH, dKH = gotas ÷ 2.
- Rótulo transcrito em varejo (só com aviso "confira a embalagem"): Tetra AquaSafe 2 gotas/L (50/100 ml) e 5 ml/38 L (250 ml); Labcon Aqualife 1 gota/L a cada 48h; Seachem Stability 5 ml/40 L dia 1 e 5 ml/80 L dias 2-7.
- PENDENTES, não viram constante: Seachem Prime (5 ml/200 L, "até 5x"); sal grosso (3 valores em 5 fontes); banho de sal (10-15 g/L 20min vs 5 g/L 15min); azul de metileno (concentração nunca declarada); bicarbonato para KH; amoníaco para fishless; todo o catálogo Alcon/Labcon (site bloqueia leitura automatizada).
- Conflito grave: substrato "1 a 2 kg por litro" (guiadoaquarismo) vs 1 kg ≈ 1 L (peixeseaquarismo) — 100% de diferença.

## Constantes com fonte já colhidas (reconfirmar no Bloco 2)
- Turnover: 5-10x/h (Aquarismo Paulista, AquaOnline); 4-5x (my-best BR); 3-5x plantado (AquaPeixes). Sump ≥ 20% do volume (AquaOnline).
- Lotação: 1 cm/L (clássica, criticada); 1,5-4 L por cm; regra dos 10%; kinguio 100 L + 40 L por adicional (Aquarismo Paulista); oscar 100 L + 100 L por peixe.
- TPA: 10-30% (Escola de Aquário), 25-30% semanal (Aquário Vivo), máx 50%, água nova com ≤1°C de diferença (>2°C choque).
- Nitrato: >40 ppm troca urgente; ≤20 ppm sensíveis (Aquário Vivo); 5-10 ppm plantado (aquariosplantados).
- Iluminação lm/L baixa/média/alta: 20/30-40/60 (peixeseaquarismo); 10-20/20-40/>40 (aquarioturbinado); 15/30/60 (aquariosplantados). Fotoperíodo 6-8h low tech, 8-10h high tech, 5-6h contra alga, 4-6h ciclagem. Kelvin 6500-8000.
- CO2: 15-35 mg/L seguro, >30-35 tóxico; drop checker azul/verde/amarelo (CO2Art).
- Carvão ativado 1-2 g/L, trocar 15-30 dias; perlon semanal a quinzenal; cerâmica regenerar 6-12 meses (Aquarismo Paulista).
- Temperatura por espécie (Petz): betta 24-28, kinguio 18-24, guppy 23-26, disco 26-30, tetra 26-30, bandeira 24-28. Barbo sumatra pH 5,0-8,0 / 20-28°C; paulistinha 18-28°C (Aquarismo Paulista).
- Ordem das mídias no canister (AquaPeixes): cerâmica/argila → perlon → carvão → perlon → cerâmica → perlon.


---

# CONSTANTES VERIFICADAS NO BLOCO 2 (07/09/2026) — acréscimo ao corpus

Todas com id, fonte, url, data e status em `dados/constantes-calculadoras.json`.

## Achados de fabricante que nenhuma fonte BR do corpus confronta
- **Turnover:** Eheim classic 250 (2213) — 440 L/h nominais para até 250 L, coluna máx. 1,5 m, 8 W, cesto 3,5 L, volume de filtragem 3,0 L, Substrat Pro de fábrica. Isso dá **1,8 renovações/h**, contra as 5-10 x/h das regras de bolso BR: divergência de 3 a 6 vezes. Virou o eixo da C3.
- **Aquecedor:** Eheim Jäger — linha 25 a 300 W (9 tamanhos, 20 a 1000 L), ajuste 18-34 °C ±0,5 °C, e o modelo de **200 W declarado para 30 a 400 L** (faixa de 13x). Serve para escolher potência comercial, nunca para dimensionar.
- **Mídia (vácuo nº 1) — ampliado em 08/09/2026 de 2 para 4 âncoras:** Seachem Matrix "250 mL para 200 L" (1,25 mL/L) E "1 L para 100 gal" (2,64 mL/L), as duas leituras da própria copy, conflitando por 2,1x, com >~700 m²/L de área; **JBL MicroMec** 650 g (= 1 L) para 200 L = **5,00 mL/L**, 1.500 m²/L; **Ocean Tech Bio Glass** 1 L para cada 80 L = **12,50 mL/L**, 1.500 m²/L, a **única marca brasileira** que publica o número. Do piso ao teto, **10 vezes**. E o paradoxo: área × dosagem = área entregue por litro de água vai de 0,88 a 18,8 m² (**21x**), com a marca de MAIOR área declarada pedindo DEZ VEZES mais mídia — a relação inversa da esperada, e nenhuma delas publica método. Segunda família, de quem vende FILTRO (mídia TOTAL): Eheim 2213 3,0 L para 250 L = **12,0 mL/L**; Seachem Tidal 55 1,2 L para 200 L = **6,0 mL/L**; Atman AT-3338 3,6 a 10,7; AT-3338S 8,8 a 26,3 (fichas de varejo ambíguas, as duas leituras publicadas). A Seachem é fabricante de mídia E de filtro, e as duas declarações dela não se falam. Páginas de fabricante bloqueadas pelo egresso: tudo colhido por busca e reconfirmado em varejo BR — reconfirmar por foto de rótulo.

## Novas constantes fora do aquarismo
- **Vidro float:** massa específica 2500 kg/m³ = **2,5 kg/m² por mm** de espessura (1 m² de 4 mm = 10 kg). Fonte: Cebrace / Saint-Gobain Sekurit, convergente em duas fontes do setor.
- **Carga de piso (ABNT NBR 6120:2019, via fontes secundárias — norma paga):** 1,5 kN/m² (~153 kgf/m²) dormitório/sala/cozinha/sanitário; 2,0 área de serviço; 3,0 corredor com acesso público. CRÍTICO: é carga distribuída de projeto, não autoriza carga concentrada de aquário. A C2 dá o número e manda consultar engenheiro — nunca "pode" ou "não pode".

## Recusas registradas (não usar em fórmula até ter fonte)
Coeficiente U do vidro do aquário · porosidade e densidade do substrato (as fontes conflitam 100 %) · espessura de vidro por litragem · "regra dos 10 %" de lotação (a fonte não define a base) · PPFD por litragem · tarifa de energia · mínima por cidade · **proporção entre as camadas do cesto** (nenhuma fonte reparte o volume em porcentagens — a C12 publica a ORDEM e recusa a proporção) · **densidade aparente do carvão ativado** (sem ela, a regra BR em g/L e a declaração do fabricante em mL/L não se comparam) · **taxa de nitrificação por área de mídia** (é a constante que fecharia a C12: as dosagens são por litro de água quando o trabalho depende da amônia) · as 7 dosagens pendentes do C13 (Prime, sal grosso, banho de sal, azul de metileno, bicarbonato, amoníaco, catálogo Alcon/Labcon).


---

# MUTIRÃO DE 09/09/2026 (3ª execução do dia) — três blocos entregues no `main`

Contrato: nasceu a **seção 14.9 do `ARQUIPELAGO.md`** — o alvo não é indexar, é a PRIMEIRA
PÁGINA, com palavra-chave que vende. Toda página nasce com a consulta-alvo escrita e a
justificativa de por que ela chega ao top 10; antes de criar, olhar a SERP e classificar quem
ocupa as 10 primeiras. A 14.6 passou a exigir "indexou **E** apareceu" para dobrar a leva. A 14.3
registrou que o Raphael descartou a meta de "60 produtos".

- **T3(a) — banco de espécies: 25 → 36.** Página de espécie apta subiu de 21 para 24; C8 de 22
  para 28; C5 de 23 para 33. Duas recusas documentadas: o **porte do gurami mel** (13,7 cm TL
  atribuído à FishBase em duas buscas, implausível e contraditório com a própria ficha) e o
  **Poecilia wingei inteiro** (sem temperatura e sem tamanho de aquário). Precedente novo: número
  atribuído que contradiz a própria ficha não entra só porque tem dono.
- **T3(a2) — iluminação: 16 → 20 luminárias**, C15 de 5 para 9 aptas, snippet 1.1.1. Entraram
  Chihiros A301/A361/A601/A801, as únicas que declaram fluxo em toda a escada. **O buraco de
  lúmen é estrutural**: 11 das 20 luminárias estão barradas por `fluxo_lm`, e o meio do mercado
  brasileiro (as 8 Soma inclusive, todas com link) não publica lúmen.
- **T3(b) — voltagem: nenhum número novo.** Chihiros WRGB II Pro 60 e Sicce Scuba Contactless
  150 W tentados, nenhuma fonte declara a tensão. São **dez elétricos com `voltagem: null`**, cada
  um com o motivo. Nada foi chutado.
- **T3(c) — cobertura de faixa: nasceu o instrumento.** `ferramentas/varrer-cobertura.mjs` abre
  cada calculadora num Chromium e a preenche ponto a ponto; `dados/cobertura-de-faixa.md` é a
  série histórica. Primeira medição: **87 faixas, 18 vazias, 28 abaixo do piso de 3, 41 ok.**
  A C15 não cumpre o critério em NENHUMA faixa; a C5 fica sem aquecedor de 310 a 400 L; a C3 não
  tem filtro nenhum de 20 a 40 L; a C12 cumpre em toda a faixa mas exatamente no piso.

**Lista de compras que substitui qualquer meta de número redondo:** (1) luminária com fluxo acima
de 80 cm; (2) aquecedor acima de 300 W; (3) filtro de 150 a 400 L/h para 20 a 40 L; (4) luminária
de 30 a 55 cm para exigência alta; (5) aquecedor de 250 W e outro de 25 a 50 W; (6) uma quarta
mídia biológica.

**Bloqueio que persiste:** `aquametria.com.br` devolve EGRESS_BLOCKED para a nuvem. Repositório na
**revisão 25**, site medido pela última vez na **11**. Quem aciona o Sync é a Sentinela Técnica no
Chrome do Raphael.

**Próximo passo: T1**, medir a indexação no Search Console — depende do Chrome, não da nuvem.


---

# T7 PAR C12 (09/09/2026, execução das 23h17Z) — a quarta calculadora ganhou as três peças da seção 5

Manifest **revisão 31**, C12 na **1.2.0**. Bloco escolhido por ser o primeiro da fila que não depende
do egresso. A ilha foi sorteada pela seção 1 do contrato — e a **primeira tentativa, na Robometria,
teve o push da reserva RECUSADO** porque outra execução commitou a reserva dela 13 segundos antes.
Rebase, volta ao passo 2, Aquametria. A reserva por commit fez exatamente o que promete.

- **As três peças, de uma vez:** resposta antes da explicação (bloco `aqm-c12-direta`, antes do
  formulário), tabela pré-renderizada de 30 a 300 L com cinco colunas, e JSON-LD no `wp_head` com
  `WebApplication` e `FAQPage` de 15 perguntas. Nenhuma fórmula mudou e nenhuma dosagem foi escolhida:
  a página continua publicando as quatro declarações com atribuição e recusando a média.
- **Tudo sai de espelho em PHP do próprio script** — `mL()`, `litros()`, `pct()`, `ancorasBio()`,
  `ancorasFiltro()`, função a função. É a mesma regra da C15 e ela não é estética: tabela servida que
  contradiz a calculadora logo acima dela é pior que tabela nenhuma.
- **O achado: em quatro dos seis volumes da escada, a dosagem mais generosa NÃO CABE no filtro.**
  104 % do cesto do Tidal 55 a 100 L, 156 % a 150 L, 208 % a 200 L, 107 % do AT-3338S a 300 L. A
  coluna do teto físico é a que não existe em português, e pré-renderizá-la tornou visível o que a
  calculadora só dizia depois de a pessoa preencher o formulário. Critério da célula: entre os filtros
  que o fabricante DECLARA para aquele volume, o de menor volume declarado — elegibilidade declarada
  antes de adequação, seção 7.
- **A constante `AQUAMETRIA_C12_VERSAO` estava mentindo na tela**: tinha ficado em `1.0.1` quando a
  1.1.0 saiu, e é ela que a página imprime. Acertada para 1.2.0.
- **VERDE FALSO NO FERRAMENTAL, e é a lição que vale para todo verificador da ilha:**
  `conferir-protecao-funcoes.py` recebe os arquivos por argumento e, chamado **sem** argumento,
  varria uma lista vazia e saía 0 em silêncio. Agora varre os snippets da própria ilha por padrão e
  **reprova quando não acha arquivo nenhum**. Controle negativo conferido. Regra: *instrumento que
  pode sair 0 sem ter medido nada não é instrumento — todo verificador precisa reprovar quando não
  mede, nunca aprovar por omissão.*
- **A C15 tinha as três peças e mesmo assim ficava FORA do teste que as mede.** O bloco de resposta
  direta dela usava `aqm-c15-citar`, a MESMA classe da caixa de citação do resultado. Ganhou
  `aqm-c15-direta` e `aqm-c15-bloco-exemplos` — localizadores, não estilo. **Peça entregue sem teste é
  peça que a próxima sessão quebra sem ninguém notar.**
- **O teste de visibilidade deixou de cravar litro como eixo.** O eixo é propriedade da CALCULADORA:
  C3, C5 e C12 respondem por volume de água, a C15 por comprimento do vidro. Exigir litro para todas
  reprovaria a C15 por estar certa — que é como um teste treina a próxima sessão a ignorá-lo. A casa
  decimal também virou opcional, porque a C12 imprime "30,0 L" com o mesmo `litros()` da calculadora,
  e exigir "30 L" cravado reprovaria a tabela por ser FIEL ao script. **De 46 para 124 afirmações, de
  2 para 4 calculadoras medidas.**
- **Custo de bancada medido, e vale para a próxima sessão:** os testes de navegador levam dezenas de
  minutos na nuvem porque cada `page.goto()` espera as fontes do Google até o timeout de 30 s, por
  navegação. **Rodar três em paralelo é pior que em série** — três Chromiums disputam a máquina.
- **A quarta asserção vencida em duas execuções, e todas do mesmo tipo.** O
  `teste-navegador-cinco.mjs` exigia que a C15 saísse SEM bloco de produto — buraco que a leva de
  catálogo do próprio 09/09 fechou. Deixou de afirmar qual estado a página deve ter (os dois são
  legítimos; quem decide é o catálogo) e passou a afirmar a promessa de cada um, mais uma afirmação
  de conjunto: nenhuma calculadora é obrigada a ter link, mas as cinco juntas não podem ficar sem
  nenhum. **Regra escrita de uma vez: afirmação que descreve o ESTADO do catálogo vence sozinha e
  reprova o trabalho da fila — afirme a promessa, nunca o estado.**
- Verificação: `php -l` com controle negativo, `conferir-protecao-funcoes.py` com controle negativo
  (9 snippets, 34 funções na C12), **124 afirmações com o JavaScript desligado (0 falha)**,
  **84 cenários da C12 e 78 da C15 em Chromium real com console limpo**, **56 afirmações das cinco calculadoras calculando**, `conferir-entidades.mjs` 0 falha com a
  C12 passando a `jsonld_ok=1`, JSON-LD analisado como JSON e conferido contra a tabela servida
  número a número, validadores de produto e de espécie sem erro novo, manifest sem sha vencido.

**NÃO CONFIRMADO NO AR:** o egresso continua bloqueando `aquametria.com.br`. Repositório na revisão
**31**, site medido pela última vez na **11** — vinte revisões paradas. Quem aciona o Sync é a
Sentinela Técnica no Chrome do Raphael.

**Próximo passo: T1** (Search Console, depende do Chrome) e **T7 leva 3 — a C1**, que é a última
calculadora sem as três peças e agora tem dois moldes prontos.
